# Versioned specifications

No wire protocol is accepted yet.

The first proposal should be added as `v1.md` through an ADR-backed task.
Implementations must not infer protocol behavior from old repository history
or UI models.
