# Agent handoff

## Delivered

- Task: XT-007, Enforce auditable harness governance
- From owner: governance-agent
- To owner or reviewer: integration-owner
- Branch: `task/XT-007`
- Worktree: `/Users/bytedance/XnnTransfer/XnnTransfer-XT-007`
- Base SHA: `a95b047f258573d0f970a29c886a04530d1bc3b2`
- Head SHA: implementation head `e444231`; final reviewed head is captured by
  `.agents/records/XT-007.json`
- Worktree clean: yes after this handoff commit
- Owned paths: `AGENTS.md`, `.agents/**`, `tool/harness/**`,
  `.github/workflows/**`, `Makefile`, and governance documentation
- Observable behavior: tasks now have tracked lifecycle records; review runs
  required verification; integration records cherry-pick provenance; only
  integration-owner acceptance can produce `done`; cleanup rejects unmapped or
  incomplete work

## Contracts

- Added or changed: task states now include `integrated`; tracked JSON records
  are durable state; local Git config is only a scheduler cache; cherry-pick
  integration requires `-x` and stable patch-ID mappings
- Compatibility impact: existing XT-001 through XT-006 task branches remain
  readable and have retrospectively audited records; new tasks must include one
  spec and one record before claim
- ADR or protocol reference: ADR 0004 backfills the accepted v1 framing and
  negotiation decision; no wire bytes or C ABI behavior changed

## Verification evidence

- Command: `tool/harness/governance_test.sh`
- Result: passed the isolated
  `claim -> in_progress -> review -> integrated -> done -> cleanup` lifecycle
- Command: `make verify`
- Result: passed governance validation and lifecycle testing, native CMake
  build and CTest 5/5, Flutter formatting/analyze, and Flutter tests 26/26
- Skipped gate and reason: none

## Residual risk

- Known limitation: GitHub branch protection and default-branch selection are
  remote repository settings and cannot be guaranteed by tracked files alone
- Follow-up task: protect the integration/default branches with all CI jobs,
  then decide when the current harness baseline should replace the legacy
  default `main` branch

## Review focus

- Files or invariants requiring close review: `agent.sh` transition authority,
  patch provenance validation, historical record accuracy, cleanup refusal,
  and truthful separation of implemented foundations from planned transfer
  behavior

## Acceptance

- Accepted by: pending integration-owner review
- Accepted at: pending integration-owner review
- Follow-up runtime state: `review`
- Integration strategy: `agent.sh integrate` using `git cherry-pick -x`
- Integrated SHA: pending integration
- Verification reference: local `make verify`; final reference is recorded by
  `agent.sh accept`
