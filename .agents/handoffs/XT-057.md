# Agent handoff

## Delivered

- Task: XT-057, fatal transcript state
- From owner: `protocol-bugfix-agent`
- To owner or reviewer: `integration-owner`
- Branch: `task/XT-057`
- Worktree: `/Users/bytedance/XnnTransfer/XnnTransfer-XT-057`
- Base SHA: `2aada91914b71088ffd20eb41640b2a7240a96e3`
- Head SHA: final pre-handoff implementation commit
  `64e18b011e132f4ab31c9a4a23a140564bf819a2`
- Worktree clean: yes after this handoff commit
- Owned paths: public protocol parser header, parser implementation, and
  protocol parser tests
- Observable behavior: a connection-fatal framing, negotiation, message-ID,
  or shared-state error terminates the parser instance before later input can
  be parsed; post-binding stream-scoped errors remain recoverable

## Contracts

- Added or changed: restored the protocol v1 section 14 failure-scope contract
  in `TranscriptParser`; the public wire format and error codes are unchanged
- Compatibility impact: valid transcripts and post-binding stream-scoped
  behavior are preserved; callers that reused a parser after a
  connection-fatal error now receive `STATE_VIOLATION`
- ADR or protocol reference: `protocol/spec/v1.md`, section 14; no ADR is
  required because this restores the existing contract

## Verification evidence

- Command: `make native-test`
- Result: all 20 native tests passed; the reproduction commit
  `40a6b44f4611df96f71c47b0795dab3f6465c00e` fails only
  `xnn_transfer_protocol_tests` at the fatal-continuation assertion
- Command: `make security-test`
- Result: all 19 sanitizer tests passed, followed by successful C API and
  protocol fuzz smoke runs
- Command: `make verify`
- Result: layout, commit policy, dependency, architecture, ABI, governance,
  native, formatting, Flutter analysis, and all 28 Flutter tests passed
- Skipped gate and reason: none

## Residual risk

- Known limitation: detached deterministic proof worktrees do not contain the
  ignored pinned vcpkg checkout. Review must provide
  `XNN_TRANSFER_VCPKG_ROOT` from the task worktree so the reproduction failure
  is the regression test rather than missing infrastructure.
- Known limitation: deterministic proof currently accepts any executable
  nonzero reproduction result other than 126/127. It does not yet bind the
  failure to a test identity or normalized failure signature.
- Follow-up task: harden defect proof infrastructure so detached gates receive
  trusted tool roots and reject unrelated build or infrastructure failures.

## Review focus

- Files or invariants requiring close review: fatality classification in
  `TranscriptParser::Complete`, terminal rejection before envelope parsing or
  message-ID mutation, connection scope before binding, and preservation of
  post-binding nonzero-stream recovery

## Acceptance

- Accepted by: pending integration-owner review
- Accepted at: pending integration-owner review
- Follow-up runtime state: transition to `review`
- Integration strategy: default squash delivery
- Integrated SHA: pending
- Verification reference: pending remote CI
