# Agent handoff

## Delivered

- Task: XT-021, discovery C ABI
- From owner: `p1-discovery-abi-agent`
- To owner or reviewer: `integration-owner`
- Branch: `task/XT-021`
- Worktree: `/Users/bytedance/XnnTransfer/XnnTransfer-XT-021`
- Base SHA: `a0a6373021c267f310c435ea5599b41c93a2af8c`
- Head SHA: implementation commit `90ce20e`; this handoff and review
  transition follow it in the submitted source range
- Worktree clean: yes after this handoff commit
- Owned paths: stable C ABI header, native bridge, discovery production
  composition, bridge tests, Dart native adapter and tests, and ADR 0008
- Observable behavior: a running engine can start and stop real system
  discovery, publish copied peer appeared/updated/expired events through the
  existing bounded queue, and reconcile overflow through revisioned fixed-size
  snapshot pages; Dart copies and decodes peer data without retaining a native
  pointer

## Contracts

- Added or changed: additive discovery start, stop, snapshot, peer event, and
  stale-snapshot status definitions in ABI v1; opaque process-local peer IDs;
  256-peer registry ceiling; eight-peer snapshot pages; 96-byte labels; copied
  IPv4/IPv6 addresses; discovery callback and shutdown barriers
- Compatibility impact: existing ABI v1 numeric values, layouts, lifecycle
  symbols, frozen caller behavior, and event queue capacity are unchanged
- ADR or protocol reference: accepts
  `docs/adr/0008-p1-operation-c-abi.md`; discovery wire and cache semantics
  remain governed by `protocol/spec/discovery-v1.md` and are unchanged

## Verification evidence

- Command: `make native-test`
- Result: all 15 native, bridge, ABI, protocol, dependency, discovery, and
  export CTest targets passed on macOS arm64
- Command: `make flutter-test`
- Result: Flutter analysis reported no issues and all 28 tests passed,
  including native-backed discovery commands/snapshot layout and copied Dart
  peer decoding
- Command: `make security-test`
- Result: all 14 ASan/UBSan targets passed; C API and protocol fuzzers
  completed 2,418,450 and 779,100 executions without failure
- Command: `make verify`
- Result: layout, commit policy, dependency provenance, architecture, ABI,
  governance lifecycle, all 15 native tests, Flutter analysis, and all 28
  Flutter tests passed
- Command: `xcrun clang-format -i ...`, `dart format lib test`, and
  `git diff --check`
- Result: owned C++ and Dart files are formatted and the task diff has no
  whitespace errors
- Skipped gate and reason: Windows and Linux native/package loading require
  remote CI; two-physical-host multicast interoperability is not reproducible
  on this macOS workstation

## Residual risk

- Known limitation: local runtime evidence covers macOS arm64 and deterministic
  registry/decoder paths. Remote CI must compile and exercise Windows/Linux
  bundles, and automated CI does not prove two-host multicast behavior
- Follow-up task: XT-026 and XT-030 must extend the accepted bounded,
  caller-owned operation ABI rather than introduce borrowed asynchronous
  pointers or Dart-selected network identity

## Review focus

- Files or invariants requiring close review: additive layout and frozen v1
  compatibility in `c_api.h`; event overflow and callback barriers in
  `event_channel.hpp`; peer ID/revision continuity in
  `discovery_bridge.hpp`; production runtime thread shutdown in `service.cpp`;
  and copied FFI decoding plus stale-snapshot retry in the Dart adapter

## Acceptance

- Accepted by: pending integration-owner review
- Accepted at: pending integration-owner review
- Follow-up runtime state: transition to `review`
- Integration strategy: default squash delivery
- Integrated SHA: pending
- Verification reference: pending remote three-platform CI
