# Agent handoff

## Delivered

- Task: XT-020, native discovery service
- From owner: `p1-native-discovery-agent`
- To owner or reviewer: `integration-owner`
- Branch: `task/XT-020`
- Worktree: `/Users/bytedance/XnnTransfer/XnnTransfer-XT-020`
- Base SHA: `8310795c8e2ab0ee87b9a51a875ea8b5685655b0`
- Head SHA: implementation commits `6f27aa3` and `d59e275`; this handoff and
  review transition follow them in the submitted source range
- Worktree clean: yes after this handoff commit
- Owned paths: `native/include/xnn_transfer/core/discovery/**`,
  `native/src/discovery/**`, and `native/tests/discovery/**`
- Observable behavior: native discovery v1 validates and encodes canonical
  advertisements, publishes per-interface announcements, observes IPv4 and
  IPv6 multicast with packet metadata, maintains bounded candidates and
  tombstones, rescans interfaces, rotates tokens after wake, expires peers on
  timers, and stops as a callback barrier

## Contracts

- Added or changed: internal native discovery interfaces for injectable
  monotonic clock, entropy, timer, datagram transport, interface monitor, and
  peer events; production Asio, OpenSSL, utf8proc, and platform adapters
  implement those interfaces
- Compatibility impact: discovery v1 wire bytes are unchanged; no C ABI,
  Flutter API, persisted schema, pairing authority, or transfer behavior was
  added or changed
- ADR or protocol reference: implements `protocol/spec/discovery-v1.md` under
  the runtime and dependency boundaries accepted in
  `docs/adr/0006-p1-native-runtime.md`

## Verification evidence

- Command: `make native-test`
- Result: all 15 native, ABI, protocol, dependency, parser/cache, and discovery
  runtime CTest targets passed on macOS arm64
- Command: `make security-test`
- Result: all 14 ASan/UBSan targets passed; the C API and protocol fuzzers
  completed 2,537,418 and 1,165,573 executions without failure
- Command: `make verify`
- Result: layout, dependency provenance, architecture, ABI, governance
  lifecycle, all 15 native tests, Flutter analysis, and all 26 Flutter tests
  passed
- Command: `xcrun clang-format --dry-run --Werror ...` and `git diff --check`
- Result: all owned C++ sources are formatted and the task diff has no
  whitespace errors
- Skipped gate and reason: Windows MSVC and Linux builds, platform monitor
  smoke tests, and packaged linkage require remote CI; they cannot run on this
  macOS workstation
- Remote failure: GitHub Actions run `31173276708` passed Linux and macOS
  native tests but the Windows native job hid Vista-era interface notification
  declarations because `_WIN32_WINNT` was not defined before Windows SDK
  headers
- Integration correction: the discovery library and runtime-test targets now
  compile with `_WIN32_WINNT=0x0601` on Windows, matching Asio's prior implicit
  baseline
- Replacement failure: GitHub Actions run `31174126739` showed that enabling
  the Windows version baseline alone did not expose the interface notification
  declarations, and Flutter's Linux subproject exposed a leaf CMake include
  path incorrectly rooted at `PROJECT_SOURCE_DIR`
- Second integration correction: Windows discovery targets now define
  consistent Win7 `WINVER`, `_WIN32_WINNT`, and `NTDDI_VERSION` baselines; the
  discovery leaf resolves its public headers from its own source directory so
  standalone and Flutter-superproject builds use the same path
- Second replacement failure: GitHub Actions run `31178010611` proved
  `netioapi.h` also requires `ws2ipdef.h` to be loaded first and showed the
  Flutter superproject compiling the discovery leaf below C++20
- Third integration correction: the discovery target explicitly requires the
  C++20 features used by its public interface and implementation; the Windows
  monitor source was reordered to load `ws2tcpip.h` before `iphlpapi.h`
- Third replacement result: GitHub Actions run `31178890490` passed every
  Linux, macOS, Flutter, sanitizer, governance, and packaged-bundle gate; both
  Windows jobs exposed that clang-format had reordered the SDK headers again
  before the correction was committed
- Fourth integration correction: clang-format is disabled only for the
  order-sensitive four-header Windows SDK block, preserving `WinSock2.h` and
  `ws2tcpip.h` before `Windows.h` and `iphlpapi.h`

## Residual risk

- Known limitation: local runtime evidence covers macOS arm64 and deterministic
  fake transport behavior; remote CI must compile and smoke the Windows and
  Linux adapters, and automated CI does not prove two-physical-host multicast
  interoperability
- Follow-up task: XT-021 owns discovery commands, bounded peer events, snapshot
  recovery, and Dart decoding through the stable C ABI; no current UI or C ABI
  path starts this service

## Review focus

- Files or invariants requiring close review: ancillary-data parsing and
  truncation handling in `asio_transport.cpp`; platform notification
  cancellation and bounded snapshots; token, sequence, jitter, wake, and
  withdrawal behavior in `service.cpp`; callback reentrancy and stop barriers;
  utf8proc canonical-label policy; and unchanged discovery v1 wire semantics

## Acceptance

- Accepted by: pending integration-owner review
- Accepted at: pending integration-owner review
- Follow-up runtime state: transition to `review`
- Integration strategy: default squash delivery
- Integrated SHA: pending
- Verification reference: pending remote three-platform CI
