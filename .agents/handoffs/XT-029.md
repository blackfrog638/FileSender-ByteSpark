# Agent handoff

## Delivered

- Task: XT-029, cancellation cleanup
- From owner: `cancellation-cleanup-agent`
- To owner or reviewer: `integration-owner`
- Branch: `task/XT-029`
- Worktree: `/Users/bytedance/XnnTransfer/XnnTransfer-XT-029`
- Base SHA: `8b048e8e6199af5808e02876d6e42285041305c2`
- Head SHA: pending delivery commit
- Worktree clean: pending delivery commit
- Owned paths: `native/include/xnn_transfer/core/transfer/**`,
  `native/src/transfer/**`, `native/tests/transfer/**`,
  `native/src/storage/**`, `native/tests/storage/**`,
  `native/src/session/**`, `native/tests/session/**`, and this task handoff
- Observable behavior: the canonical one-file sender and receiver now support
  local and remote v1 `CANCEL`/`CANCEL_ACK` from every active transfer phase.
  Cancellation stops new source and storage I/O, retains connection budget
  until a terminal acknowledgement, timeout, or shutdown, and returns a stable
  terminal tombstone. Durable commit wins preserve the destination; earlier
  cancellation aborts only the current temporary transaction. Shutdown blocks
  later frame parsing, outbox encoding, and session authorization callbacks.

## Contracts

- Added or changed: added `Cancel(now_ms)` to the internal native sender and
  receiver API, `kCancelling`/`kCancelled` states, cancelled/timeout terminal
  mapping, and a ten-second cancel acknowledgement deadline. Duplicate and
  simultaneous cancellation return the cached `CANCELLED`, `COMPLETED`, or
  existing terminal error. The first valid `CANCEL_ACK` consumes the
  outstanding cancel; later conflicting acknowledgements cannot rewrite the
  tombstone. A completion or durable commit that linearizes first remains
  completed and releases stream and directional credit exactly once.
  `SessionAuthority` now serializes activation, authorization, revocation,
  stale invalidation, and shutdown over its entry set.
- Compatibility impact: no C ABI, protocol specification, wire encoding,
  persisted schema, security profile, Flutter interface, or shared protocol
  type changed. This task implements the accepted protocol v1 cancellation
  frames and terminal codes.
- ADR or protocol reference: ADR 0002, ADR 0004, `protocol/spec/v1.md`, and
  `docs/architecture.md`; no new ADR is required for this internal native
  implementation.

## Verification evidence

- Command: focused dev build and
  `ctest --test-dir out/build/dev --output-on-failure -R
  xnn_transfer_one_file_transfer_tests`
- Result: passed, including cancellation before acceptance, during manifest
  and file phases, before commit, during commit, after completion,
  simultaneous peer cancellation, acknowledgement timeout, message-ID
  exhaustion, queued completion, repeated cancellation, shutdown races,
  temporary cleanup, source destruction, and stream/credit release
- Command: `make native-test`
- Result: passed 25/25 native tests
- Command: `make security-test`
- Result: passed 24/24 ASan/UBSan tests; C API fuzz executed 2,287,582 units
  and protocol fuzz executed 1,342,841 units without failure
- Command: `bits-code-guard` multi-pass local diff review
- Result: all recalled P1 findings were fixed; final closure reported no
  unresolved P0-P2 findings. Reports are under
  `/tmp/XnnTransfer_xt029_review_1786318452/`
- Command: `make verify`
- Result: passed the complete repository gate, including governance lifecycle,
  native 25/25, Flutter analyze, and Flutter 28/28 tests
- Skipped gate and reason: none

## Residual risk

- Known limitation: `ConnectionMessageSequence` allocates and validates
  connection-wide message IDs but does not own transport publication.
  XT-037 must serialize frames returned by concurrent engine calls, preserve
  exact publication order, schedule monotonic deadlines, and stop network
  dispatch after shutdown or a connection-fatal result.
- Known limitation: this core owns file sources, temporary transactions,
  stream tokens, credit reservations, and session authorization entries. The
  production adapter in XT-037 owns actual sockets, callbacks, worker
  executors, and TLS/context lifetime and must apply the documented engine
  executor and stop-barrier contract.
- Follow-up task: XT-037 owns the production network adapter and transport
  teardown; later acceptance tasks own cross-platform end-to-end cancellation.

## Review focus

- Files or invariants requiring close review: `sender.cpp` and `receiver.cpp`
  terminal tombstone stability, bounded queued-frame consumption, cancel ACK
  timeout, stream/credit lifetime, and completion-versus-cancel ordering;
  `receiver.cpp` durable commit preservation and single temporary abort;
  `authority.cpp` shutdown synchronization; transfer tests cover exact
  message ordering, fault injection, and deterministic commit/cancel races.

## Acceptance

- Accepted by: pending integration-owner review
- Accepted at: pending
- Follow-up runtime state: transition to `review`
- Integration strategy: default squash delivery
- Integrated SHA: pending
- Verification reference: local gates above; remote cross-platform CI pending
