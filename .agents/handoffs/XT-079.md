# Agent handoff

## Delivered

- Task: XT-079, Deterministic TDD checkpoints
- From owner: `tdd-checkpoint-agent`
- To owner or reviewer: `integration-owner`
- Branch: `task/XT-079`
- Worktree: `/Users/bytedance/XnnTransfer/XnnTransfer-XT-079`
- Base SHA: `cada8ddf299ef36fa33642342912e58fef35ee97`
- Head SHA: pending review transition; payload head `8446f7d`
- Worktree clean: yes after this handoff commit
- Owned paths: XT-079 record, task spec, and handoff; manifest and Makefile
  gate registration; `agent.sh`, governance, stale-base, and TDD proof
  harness files and tests
- Observable behavior: `tool/harness/agent.sh checkpoint XT-NNN red`
  resolves the focused trusted gate, proves base pass and attributed Red,
  scans every pre-Red commit, freezes the approved contract and oracle blobs,
  and commits only the task record with checkpoint lifecycle provenance.
  Schema-v4 review then replays base, Red, and head in detached worktrees and
  writes `verification.tdd_proof`.

## Contracts

- Added or changed: schema-v4 verification may contain generated
  `tdd_checkpoint` and `tdd_proof` objects. The proof binds the trusted command,
  plan digest, criterion IDs, failure fingerprints, proof surface, governance
  context, base/Red/checkpoint/head commits, exit codes, and output digests.
- Compatibility impact: record schemas 1-3 and the existing schema-v3 bugfix
  `defect_proof` remain unchanged. Schema-v4 bugfixes use the regression TDD
  checkpoint and bind its Red SHA to `defect.reproduction_commit`, without
  duplicating legacy `defect_proof`.
- ADR or protocol reference: implements proposed ADR 0016. No C ABI, wire
  protocol, production persistence, or security profile changed.

## Verification evidence

- Command: `python3 -B tool/harness/tdd_proof_test.py`
- Result: passed 20 tests covering CLI lifecycle commits, base/Red/head replay,
  all deterministic proof modes, full-history production detection, frozen
  oracle and governance context, registration declarations, infrastructure
  failures, task-authored evidence, ancestry, dirty runs, and cleanup.
- Command: `python3 -B tool/harness/defect_proof_test.py`
- Result: passed 22 tests; legacy deterministic bugfix proof is unchanged.
- Command: `python3 -B tool/harness/task_conflicts_test.py`
- Result: passed 16 tests, including plan, task-spec, relevant backlog, owned
  path, and global governance staleness while allowing unrelated progress.
- Command: `make governance-test`
- Result: passed the complete isolated lifecycle and acceptance simulation.
- Command: `make verify`
- Result: passed all repository gates, including Native 27/27, Flutter analyze
  with no issues, and Flutter 53/53.
- Skipped gate and reason: none.

## Residual risk

- Known limitation: this task supplies the deterministic local executor.
  Schema-v4 contracts selecting sanitizer, stress, platform-CI, or manual
  executors fail closed until a dedicated executor is implemented.
- Follow-up task: XT-081 activates schema-v4 generation and prompts at XT-083;
  XT-080 separately implements exact-candidate criterion evidence.

## Review focus

- Files or invariants requiring close review:
  - `tdd_proof.py`: Red history scans every commit and permits only the current
    record plus declared test-like paths; test registration requires an exact
    proof-surface entry, and files under `src`, `lib`, or `include` remain
    production even when their names contain `test`.
  - The Red direct child must be a generated `checkpoint` lifecycle commit that
    changes only the current task record and contains the exact frozen object.
  - Proof-surface blobs and relevant plan/task/manifest/workflow/runner context
    stay byte-stable through head; unrelated backlog entries do not invalidate
    a checkpoint.
  - Timeouts, exits 126/127, crash exits, skips, unrelated fingerprints,
    non-ancestors, dirty gates, and leaked worktrees all fail closed.
  - Schema-v3 records never enter the new runner or validator.

## Acceptance

- Accepted by: pending integration-owner review and remote CI
- Accepted at: pending
- Follow-up runtime state: transition to `review`
- Integration strategy: default squash delivery
- Integrated SHA: pending
- Verification reference: pending GitHub Actions run for integrated delivery

The submitted handoff may leave acceptance fields pending while the task is in
`review`. Final acceptance is written to `.agents/records/XT-NNN.json` by
`agent.sh accept`; do not edit a submitted task handoff solely to duplicate the
durable acceptance record.
