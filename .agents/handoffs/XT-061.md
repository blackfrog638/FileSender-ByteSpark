# Agent handoff

## Delivered

- Task: XT-061 TLS peer certificate bounds
- From owner: `tls-certificate-bounds-agent`
- To owner or reviewer: `integration-owner`
- Branch: `task/XT-061`
- Worktree: `/Users/bytedance/XnnTransfer/XnnTransfer-XT-061`
- Base SHA: `4f246338cc875ff4a5a163ea5c6610e5ef8c1b47`
- Head SHA: pending review transition
- Worktree clean: yes after the handoff commit
- Owned paths: `native/src/security/tls/tls_provider.cpp` and
  `native/tests/security/tls/tls_provider_test.cpp`
- Observable behavior: both TLS endpoint roles cap an incoming TLS 1.3
  Certificate body at 8,196 bytes before OpenSSL allocates the complete
  message, reject a peer leaf DER encoding above 4,096 bytes, and reject a
  presented certificate list containing anything other than exactly one leaf.

## Contracts

- Added or changed: implemented the certificate limits registered by XT-060.
  OpenSSL's body-sized `max_cert_list` is fixed to the 8,192-byte list ceiling
  plus the one-byte request-context length and three-byte list length.
  Verification checks the original untrusted list in addition to the verified
  chain because OpenSSL may discard unrelated certificates while building the
  latter. The architecture declaration is an in-place `tls` module refactor;
  it adds no provider, target, dependency, supersession claim, or lease.
- Compatibility impact: no public C++, C ABI, wire encoding, persistence,
  cipher, group, ALPN, pin, exporter, ticket, cache, or early-data contract
  changed. A peer outside the registered v1 certificate profile now fails the
  handshake.
- ADR or protocol reference: `protocol/spec/pairing-v1.md` section 9 and
  `docs/adr/0002-pairing-and-transport-security.md`

## Verification evidence

- Command: focused `xnn_transfer_tls_provider_tests`
- Result: passed with exact 4,095/4,096/4,097-byte DER and
  8,191/8,192/8,193-byte certificate-list cases, two-certificate lists, both
  endpoint roles, and 257-byte BIO fragmentation.
- Command: `make native-test`
- Result: passed; 22/22 native tests.
- Command: `make security-test`
- Result: passed; 21/21 ASan/UBSan tests, 2,503,143 C API fuzz executions, and
  708,088 protocol fuzz executions.
- Command: `make verify`
- Result: passed; repository layout, commit policy, dependencies,
  architecture, ABI, governance lifecycle, 22 native tests, Flutter analysis,
  and 28 Flutter tests.
- Skipped gate and reason: none. Cross-platform parity remains an acceptance
  condition for remote CI.

## Residual risk

- Known limitation: the dedicated 4,096-byte leaf check runs in OpenSSL's
  verification callback after parsing, while the enclosing 8,196-byte
  Certificate body cap bounds allocation before parsing. The tests exercise
  pinned OpenSSL 3.5.7 with in-memory fragmented transport; Windows and Linux
  compiler/runtime parity must be confirmed by CI before acceptance.
- Follow-up task: XT-025 consumes this bounded TLS provider when implementing
  authenticated pairing sessions.

## Review focus

- Files or invariants requiring close review: verify the 8,196-byte OpenSSL
  body limit maps exactly to the 8,192-byte list and 8,200-byte complete
  handshake limits; verify `X509_STORE_CTX_get0_untrusted` represents the
  original presented list in both roles; verify the post-handshake
  `SSL_get_peer_cert_chain` client/server leaf asymmetry cannot admit an extra
  certificate.

## Acceptance

- Accepted by: pending
- Accepted at: pending
- Follow-up runtime state: pending
- Integration strategy: default squash delivery
- Integrated SHA: pending
- Verification reference: pending remote CI
