# XT-022 handoff

## Delivered

- Task: XT-022, protected identity storage
- From owner: `protected-identity-agent`
- To owner or reviewer: `integration-owner`
- Branch: `task/XT-022`
- Worktree: `/Users/bytedance/XnnTransfer/XnnTransfer-XT-022`
- Base SHA: `cf49286ff409babf743d6d707c99fe405dc472b6`
- Head SHA: implementation and hardening through `fd5ecce`; this handoff and
  review transition follow it in the submitted source range
- Worktree clean: yes after this handoff commit
- Owned paths: identity public/internal headers, implementation, tests,
  identity CMake target, task record and handoff, and ADR 0009
- Observable behavior: identity creation and pairing-record mutation use a
  revisioned compare-and-exchange repository over protected storage; macOS
  Keychain and Windows Credential Manager adapters pass real lifecycle tests;
  Linux Secret Service use fails closed unless its D-Bus owner passes an
  explicit caller-provided qualification policy

## Contracts

- Added or changed: protected-store CAS API; versioned identity and pairing
  record codecs; move-only zeroizing secret buffers; shared `XNSP` revisioned
  platform envelope; macOS, Windows, and Linux protected-store factories
- Compatibility impact: this accepts the first persisted protected-identity
  schema as version 1. There is no legacy on-disk format or plaintext fallback.
  Corrupt, duplicate, rolled-back, locked, or unavailable records fail closed
  without silently regenerating trusted state.
- ADR or protocol reference: accepted ADR 0009 defines the schema, transaction
  model, platform profiles, secret lifetime, rollback handling, and Linux
  unsupported boundary

## Verification evidence

- Command: `make native-test`
- Result: passed; all 17 CTest targets passed, including identity repository,
  platform CAS, POSIX lock, and real macOS Keychain lifecycle tests
- Command: `make security-test`
- Result: passed; all 16 ASan/UBSan targets passed. C API and protocol fuzzers
  completed 2,524,220 and 1,214,716 executions without failure.
- Command: `make verify`
- Result: passed; repository, commit, dependency, architecture, ABI,
  governance, 17 native targets, Flutter analysis, and 28 Flutter tests passed
- Command: `tool/harness/desktop_bundle_test.sh debug`
- Result: passed; the macOS app bundle loaded the packaged native library and
  passed all four native-backed Dart event tests
- Command: GitHub Actions run `31231922564`
- Result: passed 10/10 jobs. Native and desktop-package jobs passed on
  Ubuntu, macOS, and Windows; the native matrix executed real Keychain and
  Credential Manager create/get/update/delete tests and linked pinned
  libsecret 0.21.7 on Linux; ASan/UBSan/fuzz, Flutter, commit, and harness jobs
  also passed.
- Skipped gate and reason: no positive Linux Secret Service runtime is claimed.
  No concrete backend identity has been reviewed as device-local and
  non-synchronizing, so an absent or rejected qualifier returns
  `storage_unavailable` before pairing can use the adapter.

## Residual risk

- Known limitation: macOS and Windows have production-adapter evidence, while
  Linux remains explicitly unsupported for pairing until a concrete Secret
  Service implementation and executable identity are qualified. OS credential
  facilities remain outside the application's rollback trust boundary; the
  repository detects revision mismatch but cannot defeat a privileged rollback
  of both credential contents and external revision state.
- Follow-up task: XT-025 must compose the accepted protected-store factory into
  pairing without bypassing CAS or secret-buffer boundaries, and must keep
  Linux pairing disabled until a separately reviewed backend qualification is
  available.

## Review focus

- Files or invariants requiring close review: zeroization and copy avoidance in
  `SecretBuffer`; repository create/update/delete revision rules; common
  envelope bounds; POSIX lock ownership/link/mode checks; Keychain
  non-synchronizing `ThisDeviceOnly` attributes and no-UI behavior; Credential
  Manager local persistence, SID-only mutex DACL, and OS-blob clearing; Linux
  encrypted session enforcement, D-Bus owner pinning, default-collection and
  prompt rejection, and qualifier failure behavior

## Acceptance

- Accepted by: pending integration-owner review
- Accepted at: pending integration-owner review
- Follow-up runtime state: review
- Integration strategy: default squash delivery
- Integrated SHA: pending
- Verification reference: GitHub Actions run `31231922564`
