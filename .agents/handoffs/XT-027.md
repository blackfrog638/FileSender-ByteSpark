# Agent handoff

## Delivered

- Task: XT-027 Safe one file storage
- From owner: safe-storage-agent
- To owner or reviewer: integration owner
- Branch: task/XT-027
- Worktree: /Users/bytedance/XnnTransfer/XnnTransfer-XT-027-corrected
- Base SHA: b8ebe5dd2e4d60ce46bde4770cb781df4e80f3d5
- Head SHA: e5f73c263a2188610163b1580584f813616a915b (verified payload
  before handoff-only commits)
- Worktree clean: yes during review submission; four unrelated formatting-only
  edits are preserved in named stashes and restored after lifecycle commands.
- Owned paths: native/include/xnn_transfer/core/storage/**, native/src/storage/**, native/tests/storage/**
- Observable behavior: Validated one-file requests stage bounded data under an
  exclusively locked destination root, verify exact size and injected
  integrity, reject traversal, links, reparse points, races, and collisions,
  and atomically publish without replacement.
- POSIX behavior: uses openat/mkdirat with O_NOFOLLOW, owner-only trusted
  directories without macOS extended ACLs, an exact 0600 ACL-free lock, linkat
  no-replace publication, durable stale cleanup, and fsync of the file,
  destination parent, temporary directory, and each traversed parent.
- Windows behavior: supports local fixed NTFS only; uses handle-relative
  NtCreateFile traversal with OBJ_DONT_REPARSE, retains ancestor handles
  without FILE_SHARE_DELETE, protects staging with a current-user-only DACL,
  publishes through FileRenameInformationEx with no replace, queries free space
  through the volume GUID path, flushes both affected directories, and removes
  bounded stale part-* batches without following junction targets.
- Review corrections: destination validation reserves the backend-private
  .xnn-transfer-tmp root component with Windows-safe case folding; bounded
  Windows cleanup makes durable progress before returning busy; quota,
  reparse, and missing-root errors retain their defined platform semantics.

## Contracts

- Added or changed: Added validator-only path/request capabilities, temporary
  budget and transaction states, PlatformBackend, and OpenFilesystemBackend;
  replaced the canonical xnn_transfer_storage placeholder in place.
- Compatibility impact: No C ABI, wire protocol, persisted schema, or
  cross-workstream shared type changed. Unsupported Windows filesystems,
  network shares, and non-fixed volumes fail closed with kUnsupported.
- ADR or protocol reference: ADR not required by the task record; the
  implementation preserves the existing protocol/spec/v1.md path and size
  contract.

## Verification evidence

- Command: python3 native/tests/storage/generate_manifest_fixture_cases.py --input protocol/testdata/v1/manifest/vectors.json --check
- Result: passed; all 66 XT-011 cases classified (storage=28, XT-028=11, XT-033=27).
- Command: cmake --build out/build/dev --target xnn_transfer_storage_tests
- Result: passed with AppleClang warning gates enabled.
- Command: ctest --test-dir out/build/dev -R '^xnn_transfer_storage_' --output-on-failure
- Result: passed 2/2 focused storage tests on the final local payload.
- Command: make native-test
- Result: passed 19/19 native tests after the integration-owner review fixes.
- Command: make security-test
- Result: passed 18/18 ASan/UBSan tests and both fuzz smoke runs after the
  integration-owner review fixes.
- Command: make architecture-test
- Result: passed 17/17 architecture tests.
- Command: GitHub Actions CI run 31270977550 on windows-2022, macos-latest,
  and ubuntu-latest.
- Result: passed 10/10 jobs. Windows native passed 19/19 tests, including real
  commit, no-replace collision, junction containment, junction replacement
  race, DACL rejection, Unicode root, and stale regular/junction cleanup.
- Command: GitHub Actions CI run 31272249214 on the final no-instrumentation
  payload.
- Result: passed 10/10 jobs at d734ca98f8972bc1f38967df2906f2eb0bd4d28e.
- Command: bits-code-guard integration-owner review over
  b8ebe5dd2e4d60ce46bde4770cb781df4e80f3d5..a173144e0b2e8847ed56148ad7fb6d41933142e3
- Result: found five P1 and two P2 defects covering the private namespace,
  macOS ACLs, Windows volume/quota/reparse handling, bounded stale cleanup,
  POSIX stale cleanup durability, and missing-root classification. All seven
  were corrected by e5f73c263a2188610163b1580584f813616a915b.
- Command: GitHub Actions CI run 31274812296 on the review-corrected payload.
- Result: passed all 10 jobs at
  e5f73c263a2188610163b1580584f813616a915b, including Windows native and
  Windows desktop bundle jobs.
- Command: make verify
- Result: passed in the clean task worktree at
  e5f73c263a2188610163b1580584f813616a915b after review correction; includes
  governance lifecycle, 19 native tests, and 28 Flutter tests.
- Skipped gate and reason: no required executable gate was skipped. Hosted CI
  cannot simulate physical power loss, so it is not claimed as crash-hardware
  durability evidence.

## Residual risk

- Known limitation: hosted CI proves API behavior and flush ordering on Windows
  Server 2022, not physical power-loss durability. Windows support is limited
  to local fixed NTFS; UNC/SMB, ReFS, FAT/exFAT, and removable volumes fail
  closed. POSIX publication requires same-filesystem hard-link support, and
  free space can change after preflight.
- Follow-up task: no mandatory follow-up for the accepted local fixed
  NTFS/POSIX scope. A future filesystem-expansion task must add filesystem-
  specific atomicity and durability evidence before broadening support.

## Review focus

- Files or invariants requiring close review: POSIX exact 0600 lock metadata,
  ACL-free trusted directories, stale-cleanup syncing, openat/O_NOFOLLOW
  traversal, and linkat publication; Windows fail-closed NT resolution,
  private DACL, OBJ_DONT_REPARSE opens, bounded cleanup progress, ancestor
  handles that share reads/writes but not deletes, native no-replace rename,
  directory flush, and junction cleanup; transaction cleanup-unresolved and
  outcome-uncertain accounting.

## Acceptance

- Accepted by: pending integration owner
- Accepted at: pending
- Follow-up runtime state: review after final verification
- Integration strategy: squash through agent.sh
- Integrated SHA: pending
- Verification reference: local focused/native/security/architecture, clean
  make verify, and final cross-platform CI run 31274812296 all passed.
