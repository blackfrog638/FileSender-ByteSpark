# Agent handoff

## Delivered

- Task: XT-027 Safe one file storage
- From owner: safe-storage-agent
- To owner or reviewer: integration owner
- Branch: task/XT-027
- Worktree: /Users/bytedance/XnnTransfer/XnnTransfer-XT-027-corrected
- Base SHA: b8ebe5dd2e4d60ce46bde4770cb781df4e80f3d5
- Head SHA: e170e755ecc6bc238bacad2e8f0b3261222355f8
- Worktree clean: yes before this handoff commit
- Owned paths: native/include/xnn_transfer/core/storage/**, native/src/storage/**, native/tests/storage/**
- Observable behavior: Validated one-file requests stage bounded data under an exclusively locked destination root, verify exact size and injected integrity, reject links and collisions, and atomically publish with no replacement on supported POSIX filesystems.

## Contracts

- Added or changed: Added validator-only path/request capabilities, temporary budget and transaction state contracts, the PlatformBackend interface, and OpenFilesystemBackend; replaced xnn_transfer_storage in place with a STATIC target.
- Compatibility impact: No C ABI, wire protocol, persisted schema, or shared cross-workstream type changed; Windows returns kUnsupported instead of claiming unsafe storage.
- ADR or protocol reference: ADR not required by the task record; behavior implements the existing protocol/spec/v1.md path and size contract without changing it.

## Verification evidence

- Command: python3 native/tests/storage/generate_manifest_fixture_cases.py --input protocol/testdata/v1/manifest/vectors.json --check
- Result: passed; all 66 XT-011 cases classified (storage=28, XT-028=11, XT-033=27).
- Command: cmake --build out/build/dev --target xnn_transfer_storage_tests
- Result: passed with AppleClang warning gates enabled.
- Command: ctest --test-dir out/build/dev -R '^xnn_transfer_storage_' --output-on-failure
- Result: passed 2/2 focused storage tests.
- Command: make native-test
- Result: passed 19/19 native tests.
- Command: make security-test
- Result: passed 18/18 ASan/UBSan tests and both fuzz smoke runs.
- Command: make architecture-test
- Result: passed 17/17 architecture tests.
- Command: make verify
- Result: passed every repository verification gate, including native, Flutter, ABI, dependency, architecture, governance, and commit policy checks.
- Skipped gate and reason: Coverage percentage skipped because the repository and task define no coverage threshold and EXEC_SOURCE is empty.

## Residual risk

- Known limitation: Windows has no reparse-safe backend and fails closed with kUnsupported. POSIX commit requires a same-filesystem hard-link-capable destination; unsupported filesystems fail without publishing. Free-space is checked before creation but can still change before writes complete, so ENOSPC remains a handled runtime failure.
- Follow-up task: Create a dedicated Windows native storage task for handle-relative reparse-safe traversal, no-replace rename, restart cleanup, and Windows CI coverage.

## Review focus

- Files or invariants requiring close review: native/src/storage/filesystem_backend.cpp ownership transfer on commit outcomes, openat/O_NOFOLLOW traversal, startup cleanup lock, and linkat no-replace durability; native/src/storage/receive_transaction.cpp unresolved-budget accounting.

## Acceptance

- Accepted by: pending integration owner
- Accepted at: pending
- Follow-up runtime state: review
- Integration strategy: pending integration owner
- Integrated SHA: pending
- Verification reference: pending review transition
