# Agent handoff

## Delivered

- Task: XT-027 Safe one file storage
- From owner: safe-storage-agent
- To owner or reviewer: integration owner
- Branch: task/XT-027
- Worktree: /Users/bytedance/XnnTransfer/XnnTransfer-XT-027-corrected
- Base SHA: b8ebe5dd2e4d60ce46bde4770cb781df4e80f3d5
- Head SHA: f5c96e10c760eff8bcf06ea079cb5d52bf4a00b9
- Worktree clean: yes at the corrected core commit before this handoff update
- Owned paths: native/include/xnn_transfer/core/storage/**, native/src/storage/**, native/tests/storage/**
- Observable behavior: Validated one-file requests stage bounded data under an exclusively locked destination root, require lock metadata permission bits exactly 0600, verify exact size and injected integrity, reject links and collisions, and atomically publish with no replacement on supported POSIX filesystems. Every traversed intermediate destination entry has its containing parent synced before descent, including entries left by an earlier failed attempt.

## Contracts

- Added or changed: Added validator-only path/request capabilities, temporary budget and transaction state contracts, the PlatformBackend interface, and OpenFilesystemBackend; replaced xnn_transfer_storage in place with a STATIC target. Corrected POSIX lock metadata validation and nested-directory crash durability without changing a public contract.
- Compatibility impact: No C ABI, wire protocol, persisted schema, or shared cross-workstream type changed; Windows returns kUnsupported instead of claiming unsafe storage.
- ADR or protocol reference: ADR not required by the task record; behavior implements the existing protocol/spec/v1.md path and size contract without changing it.

## Verification evidence

- Command: python3 native/tests/storage/generate_manifest_fixture_cases.py --input protocol/testdata/v1/manifest/vectors.json --check
- Result: passed; all 66 XT-011 cases classified (storage=28, XT-028=11, XT-033=27).
- Command: cmake --build out/build/dev --target xnn_transfer_storage_tests
- Result: passed with AppleClang warning gates enabled.
- Command: ctest --test-dir out/build/dev -R '^xnn_transfer_storage_' --output-on-failure
- Result: passed 2/2 focused storage tests on f5c96e1 payload; includes a real-filesystem negative test rejecting a preexisting 0644 lock.
- Command: make native-test
- Result: passed 19/19 native tests on the final corrected payload.
- Command: make security-test
- Result: passed 18/18 ASan/UBSan tests and both fuzz smoke runs on the final corrected payload.
- Command: make architecture-test
- Result: passed 17/17 architecture tests.
- Command: make verify
- Result: not counted; the integration owner killed the run while three governance suites were concurrent. The wrapper returned zero after SIGKILL, so this is not verification evidence.
- Skipped gate and reason: Final serial make verify is intentionally deferred to the integration-owner main thread after XT-023. Coverage percentage was skipped because the repository and task define no coverage threshold and EXEC_SOURCE is empty.

## Residual risk

- Known limitation: Windows has no handle-relative reparse-safe backend and fails closed with kUnsupported. This is honest but does not satisfy XT-027 acceptance. POSIX commit requires a same-filesystem hard-link-capable destination; unsupported filesystems fail without publishing. Free-space is checked before creation but can still change before writes complete, so ENOSPC remains a handled runtime failure.
- Follow-up task: Block XT-027 until a Windows handle-relative backend provides reparse-safe traversal, no-replace atomic commit, restart cleanup, and real Windows CI coverage.

## Review focus

- Files or invariants requiring close review: native/src/storage/filesystem_backend.cpp descriptor ownership on invalid lock metadata and parent-fsync errors, exact 0600 lock permissions, unconditional parent syncing before intermediate-directory descent, openat/O_NOFOLLOW traversal, startup cleanup lock, and linkat no-replace durability; native/src/storage/receive_transaction.cpp unresolved-budget accounting.

## Acceptance

- Accepted by: pending integration owner
- Accepted at: pending
- Follow-up runtime state: blocked
- Integration strategy: do not integrate while blocked
- Integrated SHA: pending
- Verification reference: focused/native/security passed locally; final serial make verify pending integration owner
