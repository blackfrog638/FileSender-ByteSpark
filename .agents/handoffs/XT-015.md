# Agent handoff

## Delivered

- Task: XT-015, Enforce Ed25519 subgroup validation
- From owner: ed25519-subgroup-agent
- To owner or reviewer: integration-owner and XT-010 independent reviewer
- Branch: `task/XT-015`
- Worktree: `/Users/bytedance/XnnTransfer/XnnTransfer-XT-015`
- Base SHA: `b361412a3d1a5bf425044c55cb7ac984a43e2284`
- Head SHA: implementation commit
  `cc3a1e3f3108d87458d4a1a62df318308709c4ff`; the submitted review head also
  includes this handoff and is recorded by the review transition
- Worktree clean: yes after the implementation commit; this handoff is the
  subsequent task-owned metadata commit
- Owned paths: `protocol/testdata/security/v1/**`, ADR 0002, and this task's
  record/handoff
- Observable behavior: the fixture oracle accepts an Ed25519 identity or
  rotation key only after canonical decoding, `P != identity`, and a real
  `[L]P = identity` scalar-multiplication check; it validates the unchanged 17
  positive vectors and 37 negative vectors

## Contracts

- Added or changed: proposed ADR 0002 now normatively defines accepted
  Ed25519 keys as non-identity members of the prime-order subgroup before TLS
  identity acceptance, possession verification, transcript hashing, pinning,
  device-ID derivation, rotation, or durable trust
- Compatibility impact: all 17 RFC TEST 1/2/3-bound positive outputs are
  byte-exact; all 34 pre-existing rejection codes are unchanged; three new
  identity, order-2, and mixed-order cases reject as `INVALID_PUBLIC_KEY`
- ADR or protocol reference: ADR 0002 remains `proposed`; this task adds
  proposed-profile fixture evidence only and does not claim production
  Ed25519, TLS, pairing, transport, or cross-platform conformance

## Verification evidence

- Command: `python3 protocol/testdata/security/v1/validate_vectors.py`
- Result: passed 17 positive and 37 negative vectors, including identity,
  non-identity order-2, and mixed-order subgroup rejection
- Command: independent inline Python affine Edwards25519 decoder, scalar
  multiplication, and cofactored identity-signature equation check
- Result: RFC TEST 1/2/3 were non-identity prime-subgroup points; identity,
  order-2, and mixed-order matched the BR-04 membership table; the forged
  identity signature equation held for the empty message and two distinct
  nonempty messages
- Command: structured `jq`/`diff` comparison with the pre-task
  `vectors.json`
- Result: the positive-output diff was empty and no pre-existing negative
  vector changed its expected rejection code
- Command: `make verify`
- Result: passed repository layout, 15-task governance and lifecycle, all 7
  native CTest targets, Flutter formatting/analyze, and all 26 Flutter tests
- Command: `jq empty protocol/testdata/security/v1/vectors.json` and
  `git diff --check`
- Result: passed
- Skipped gate and reason: none

## Residual risk

- Known limitation: the validator is deterministic proposed-profile fixture
  evidence, not production Ed25519 or a TLS identity implementation; the
  OpenSSL, Node, CryptoKit, and libsodium divergence is pinned from XT-010
  BR-04 and was not rerun as live macOS, Windows, and Linux TLS conformance by
  this task
- Follow-up task: XT-010 must independently reproduce the corrected subgroup
  and forgery checks and decide whether BR-04 is closed; live platform TLS
  conformance remains an implementation prerequisite

## Review focus

- Files or invariants requiring close review: the complete extended-coordinate
  addition formula and `[L]P` identity test must remain a true membership
  computation; mixed-order coverage must not be replaced by a small-order
  blacklist or `[8]P != identity`; ADR status must remain `proposed`

## Acceptance

- Accepted by: pending integration-owner review
- Accepted at: pending integration-owner review
- Follow-up runtime state: transition to `review`; do not integrate, accept, or
  clean up from this task worktree
- Integration strategy: pending `agent.sh integrate` using `git cherry-pick -x`
- Integrated SHA: pending
- Verification reference: pending `agent.sh transition XT-015 review`
