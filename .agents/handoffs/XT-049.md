# Agent handoff

## Delivered

- Task: XT-049, protocol public headers
- From owner: `protocol-boundary-agent`
- To owner or reviewer: `integration-owner`
- Branch: `task/XT-049`
- Worktree: `/Users/bytedance/XnnTransfer/XnnTransfer-XT-049`
- Base SHA: `83a571ec0e8393bae84360b14d5edd2271c9623b`
- Head SHA: implementation commit `852457ab2d808f8eb536332cb5a4c576048f0d50`
- Worktree clean: yes after this handoff commit
- Observable behavior: consumers include the parser from
  `xnn_transfer/protocol/v1_parser.hpp`; production targets cannot expose a
  resolved `native/src` directory through PUBLIC or INTERFACE CMake visibility

## Contracts

- Added or changed: moved the parser header to the canonical public include
  tree, restricted the protocol target to `native/include`, registered that
  root in the module inventory, and added CMake visibility checks
- Compatibility impact: the internal include path changed; parser namespaces,
  types, signatures, wire behavior, C ABI, and persisted formats are unchanged
- ADR or protocol reference: implements the existing module boundary in
  `docs/architecture.md`; no ADR or protocol specification change is required

## Verification evidence

- Command: `make architecture-test`
- Result: all 17 architecture tests passed, including PUBLIC and INTERFACE
  native source-directory rejection
- Command: `make native-test`
- Result: all 15 native, ABI, protocol, dependency, security-vector, and
  discovery CTest targets passed on macOS arm64
- Command: `make verify`
- Result: layout, commit policy, dependency, architecture, ABI, governance,
  native, Flutter analysis, and all Flutter tests passed
- Command: `cmake --preset fuzz && cmake --build --preset fuzz --target xnn_transfer_protocol_fuzzer`
- Result: the parser and fuzzer translation units compiled with the canonical
  public header; final linking could not run because this Xcode installation
  lacks `libclang_rt.fuzzer_osx.a`

## Residual risk

- Known limitation: local evidence covers macOS arm64; Linux and Windows CMake
  path behavior requires remote CI, and the local libFuzzer runtime is missing
- Follow-up task: none; future native modules should expose public headers only
  from `native/include` and keep source-local headers PRIVATE

## Review focus

- Files or invariants requiring close review: CMake generator-expression
  normalization, PUBLIC versus PRIVATE visibility handling, removal of the old
  parser header, and unchanged parser/fuzzer compilation

## Acceptance

- Accepted by: pending integration-owner review
- Accepted at: pending integration-owner review
- Follow-up runtime state: transition to `review`
- Integration strategy: default squash delivery
- Integrated SHA: pending
- Verification reference: pending remote three-platform CI
