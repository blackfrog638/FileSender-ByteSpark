# Agent handoff

## Delivered

- Task: XT-043, ABI compatibility gate
- From owner: `abi-gate-agent`
- To owner or reviewer: `integration-owner`
- Branch: `task/XT-043`
- Worktree: `/Users/bytedance/XnnTransfer/XnnTransfer-XT-043`
- Base SHA: `a67320a`
- Head SHA: implementation commit `5f8ee0d`; this handoff and review transition
  follow it in the submitted source range
- Worktree clean: yes after this handoff commit
- Owned paths: `AGENTS.md`, `.agents/**`, `Makefile`, `tool/harness/**`,
  `.github/workflows/ci.yml`, `native/CMakeLists.txt`, and
  `native/tests/abi/**`
- Observable behavior: native verification rejects changes to ABI v1
  constants, enum values, struct field types or prefix layout, function
  signatures, required dynamic-library exports, and legacy caller behavior

## Contracts

- Added or changed: internal `make abi-compat-test` gate, a frozen ABI v1 caller
  header, compile-time compatibility assertions, an executable legacy client,
  binary export resolution, and positive and negative compile fixtures
- Compatibility impact: no production C ABI changed; additive symbols and
  aligned struct tail fields remain permitted by the compatibility assertions
- ADR or protocol reference: enforces the accepted ABI v1 policy in ADR 0001
  and ADR 0003 without changing either decision

## Verification evidence

- Command: `make abi-compat-test`
- Result: all 5 ABI tests passed, covering current and frozen layouts, the
  legacy lifecycle client, 8 required exports, one additive fixture, and 4
  rejected breaking fixtures
- Command: `make security-test`
- Result: all 12 ASan/UBSan CTest targets passed; the C API and protocol fuzzers
  completed about 2.72 million and 0.87 million iterations without failure
- Command: `make verify`
- Result: architecture and governance gates, all 13 native CTest targets,
  Flutter analysis, and all 26 Flutter tests passed
- Skipped gate and reason: Windows and Linux ABI execution require their
  respective remote CI runners and remain pending at handoff

## Residual risk

- Known limitation: the frozen layouts intentionally require 64-bit pointers
  and `size_t`; adding a supported 32-bit target requires a separately reviewed
  ABI baseline rather than weakening these assertions
- Follow-up task: none; integration review must confirm the native CI matrix is
  green on macOS, Windows, and Linux before acceptance

## Review focus

- Files or invariants requiring close review: frozen v1 header fidelity,
  all field offsets and function signatures in `v1_compat_assertions.hpp`,
  Windows `dllimport` linkage, platform-neutral export lookup, sanitizer export
  probe exclusion, and native CI matrix execution

## Acceptance

- Accepted by: pending integration-owner review
- Accepted at: pending integration-owner review
- Follow-up runtime state: transition to `review`
- Integration strategy: default squash delivery
- Integrated SHA: pending
- Verification reference: pending review transition
