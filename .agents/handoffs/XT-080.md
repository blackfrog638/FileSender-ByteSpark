# Agent handoff

## Delivered

- Task: XT-080, Criterion evidence closure
- From owner: `criterion-evidence-agent`
- To owner or reviewer: `integration-owner`
- Branch: `task/XT-080`
- Worktree: `/Users/bytedance/XnnTransfer/XnnTransfer-XT-080`
- Base SHA: `bf611967420b70ac0069825e1649a79c3cfec9b9`
- Head SHA: pending review transition; implementation head `a201539`
- Worktree clean: yes after this handoff commit
- Owned paths: XT-080 record and handoff; manifest, workflow, Makefile,
  acceptance shell, governance, GitHub CI, evidence runner, and focused tests
- Observable behavior: schema-v4 acceptance reruns every producer-owned trusted
  evidence gate on the integrated SHA, expands the approved platform and role
  matrix into GitHub jobs, validates exact run/job/artifact provenance, and
  stores one canonical `criterion_evidence` bundle before publication.

## Contracts

- Added or changed: schema-v4 done records contain generated
  `criterion_evidence` schema version 1. The bundle binds the approved plan,
  criterion and evidence IDs, trusted command and workflow digests, source SHA,
  GitHub run ID and attempt, scenario/assertion sets, matrix jobs, runtime
  witness, packaged binary digests, artifact digests, result, and URLs.
- Compatibility impact: schema 1-3 tasks retain the XT-068 workflow and never
  require or accept criterion evidence. Schema-v4 implementation tasks collect
  only contracts naming them as `producer_task`; acceptance tasks recollect all
  mapped contracts from their own integrated candidate.
- ADR or protocol reference: implements proposed ADR 0016. No product runtime,
  C ABI, wire protocol, persisted product format, or security profile changed.

## Verification evidence

- Command: `make evidence-test`
- Result: passed 12 fixtures covering exact candidate collection, platform and
  role closure, non-producer tasks, stale SHA and run attempts, missing/skipped
  jobs, truncated API pages, duplicate scenarios, altered artifacts, binary
  mismatch, weak E2E topology, generic verify substitution, and bundle tamper.
- Command: `python3 -B tool/harness/github_ci_test.py`
- Result: passed 15 tests, including structured job/artifact/archive requests,
  bounded downloads, exact workflow selection, API failure redaction, timeout,
  malformed response, and terminal failure.
- Command: `make governance-test`
- Result: passed the complete isolated lifecycle, including legacy delivery and
  acceptance CI failure rollback, candidate cleanup, and resumed publication.
- Command: `make verify`
- Result: passed all repository gates; Native 27/27, Flutter analyze with no
  issues, and Flutter 53/53.
- Skipped gate and reason: none.

## Residual risk

- Known limitation: real GitHub artifact collection cannot execute for this
  bootstrap schema-3 task. It is covered by structured fixtures; the first
  schema-v4 delivery will exercise the live `upload-artifact@v4` API contract.
  Evidence matrices are capped at 64 jobs and one 100-item API page. Process or
  packaged E2E gates must write the bounded runtime/binary witness named by
  `XNN_EVIDENCE_WITNESS`; missing witnesses fail closed.
- Follow-up task: XT-081 activates schema-v4 generation, claim/prompt checks,
  and the XT-083 future-only boundary.

## Review focus

- Files or invariants requiring close review:
  - `evidence.py`: approved contract selection, exact matrix closure, ZIP and
    path bounds, runtime topology checks, binary hashing, canonical bundle
    validation, and source/run-attempt attribution.
  - `github_ci.py`: API requests remain bounded and secret-free, and evidence
    is collected only from the selected exact branch/SHA/run attempt.
  - `agent.sh` and `governance.py`: failed collection or acceptance CI restores
    `integrated`, leaves the protected branch unchanged, and removes generated
    evidence; legacy records never enter the new path.
  - `ci.yml`: schema-3 and done records produce an empty optional matrix, while
    integrated schema-v4 records run one trusted job per approved matrix cell.

## Acceptance

- Accepted by: pending integration-owner review and remote CI
- Accepted at: pending
- Follow-up runtime state: transition to `review`
- Integration strategy: default squash delivery
- Integrated SHA: pending
- Verification reference: pending GitHub Actions run for integrated delivery

The submitted handoff may leave acceptance fields pending while the task is in
`review`. Final acceptance is written to `.agents/records/XT-NNN.json` by
`agent.sh accept`; do not edit a submitted task handoff solely to duplicate the
durable acceptance record.
