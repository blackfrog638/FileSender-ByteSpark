# Agent handoff

## Delivered

- Task: XT-060, pairing control wire contract
- From owner: `pairing-wire-contract-agent`
- To owner or reviewer: `integration-owner`
- Branch: `task/XT-060`
- Worktree: `/private/tmp/XnnTransfer-XT-060`
- Base SHA: `b42c301c1039a986bc060f64f8e8df2cf84e4f03`
- Head SHA: pending review transition
- Worktree clean: yes after the delivery commit
- Owned paths: pairing v1 specification, protocol v1 compatibility text,
  pairing-control golden vectors and validator, security-vector documentation,
  negative-test evidence, and ADR 0002
- Observable behavior: the repository now registers one byte-exact
  pairing-control profile with canonical `XNNP` framing, deterministic
  negotiation, bounded pre-trust processing, explicit decision/commit
  linearization, and a fully bound established reconnect

## Contracts

- Added or changed: registered first-pairing ALPN
  `xnn-transfer-pairing/1`, established ALPN `xnn-transfer/1`, security profile
  `0x0001`, five `XNNP` message types, stable public/local errors, resource
  ceilings, deadlines, replay handling, and terminal state transitions
- Compatibility impact: v1.0 rejects unknown, duplicate, reordered, missing,
  trailing, noncanonical, or reinterpreted bytes; a future incompatible
  pairing profile requires a distinct ALPN or profile identifier
- ADR or protocol reference: `protocol/spec/pairing-v1.md`,
  `protocol/spec/v1.md`, and
  `docs/adr/0002-pairing-and-transport-security.md`

## Verification evidence

- Command: `python3 protocol/testdata/security/v1/validate_vectors.py`
- Result: 17 existing positive and 37 existing negative security-profile
  vectors passed; 5 positive and 22 hostile pairing-control vectors passed
- Command: `make security-test`
- Result: all 19 sanitizer/native security tests passed; C API fuzz completed
  2,549,989 units and protocol fuzz completed 1,179,909 units after rebase
- Command: `make verify`
- Result: repository layout, commit policy, pinned dependencies, architecture,
  ABI, governance lifecycle, all 20 native tests, Flutter analysis, and all 28
  Flutter tests passed on macOS arm64
- Command: stable patch-id comparison of the accepted canonical product
  payload with the corrected-identity replay, excluding task record and
  handoff metadata
- Result: both product payloads equal
  `de22d837ff05f0fba64d21e0be766e7c4bce50c1`.
- Skipped gate and reason: none

## Residual risk

- Known limitation: this task provides specification and deterministic fixture
  evidence only; it does not implement TLS certificate enforcement, timers,
  protected-store commits, a native session, C ABI, or Flutter pairing flow
- Follow-up task: create a governed TLS task after XT-061 for the typed
  same-port pairing/established ALPN demultiplexer and inbound active-pin
  resolver, then make XT-025 depend on it; the current single-ALPN,
  caller-supplied optional-pin API is not implementation evidence

## Review focus

- Files or invariants requiring close review: exact ALPN/profile bytes;
  one-to-one mapping from accepted `XNNP` fields to ADR 0002 kind-`01` and
  kind-`02`; confirmation versus durable commit linearization; certificate,
  inbound byte/message, admission, replay, and deadline scopes; fresh
  established transport binding through kind-`04` and both finished values

## Acceptance

- Accepted by: pending integration-owner review
- Accepted at: pending integration-owner review
- Follow-up runtime state: transition to `review`
- Integration strategy: default squash delivery
- Integrated SHA: pending
- Verification reference: pending remote CI
