# Agent handoff

## Delivered

- Task: XT-016, Normalize security wordlist line endings
- From owner: `windows-ci-agent`
- To owner or reviewer: `integration-owner`
- Branch: `task/XT-016`
- Worktree: `/Users/bytedance/XnnTransfer/XnnTransfer-XT-016`
- Base SHA: `87566460e044a012d065fb2699bd2a3ee1408475`
- Head SHA: implementation commit
  `0e78f14`; the review transition records the submitted metadata head
- Worktree clean: yes after the handoff commit and review transition
- Owned paths: security-profile fixtures, protocol CTest registration, and
  this task's record/handoff
- Observable behavior: the security-profile oracle validates the same pinned
  word-list SHA from LF and CRLF checkouts and rejects bare carriage returns

## Contracts

- Added or changed: the word-list fixture digest is defined over canonical
  LF-delimited ASCII bytes; full CRLF checkout conversion is normalized before
  hashing and parsing
- Compatibility impact: all 17 positive outputs and 37 negative error
  contracts remain unchanged; no wire, C ABI, persisted, or runtime behavior
  changes
- ADR or protocol reference: ADR 0002 remains accepted and unchanged; the
  security-profile fixture README documents the host-independent encoding rule

## Verification evidence

- Command:
  `python3 protocol/testdata/security/v1/test_wordlist_line_endings.py`
- Result: passed LF, CRLF, and bare-CR fail-closed cases
- Command: `python3 protocol/testdata/security/v1/validate_vectors.py`
- Result: passed 17 positive and 37 negative security-profile vectors
- Command: `make native-test`
- Result: passed all 8 CTest targets, including the new line-ending regression
- Command: `make verify`
- Result: passed governance for 16 tasks, all 8 CTest targets, Flutter
  formatting/analyze, and all 26 Flutter tests
- Command: `make security-test`
- Result: ASan/UBSan tests passed; C ABI fuzzer executed 2,641,103 units and
  protocol fuzzer executed 758,013 units without a crash
- Command: `python3 -m py_compile ...` and `git diff --check`
- Result: passed
- Skipped gate and reason: Windows GitHub Actions is pending integration and
  push; it cannot be claimed from a macOS task worktree

## Residual risk

- Known limitation: the task proves deterministic fixture handling, not
  production TLS, pairing, storage, or transfer behavior
- Follow-up task: integration owner must confirm
  `Native (windows-latest)` and the other eight CI jobs pass on the integrated
  `harness` commit

## Review focus

- Files or invariants requiring close review: normalize only `CRLF` to `LF`;
  reject any remaining bare `CR`; hash the normalized bytes before parsing;
  keep the regression registered in CTest on every platform

## Acceptance

- Accepted by: pending integration-owner review
- Accepted at: pending integration-owner review
- Follow-up runtime state: transition to `review`
- Integration strategy: pending `agent.sh integrate` with `cherry-pick -x`
- Integrated SHA: pending
- Verification reference: pending review transition
