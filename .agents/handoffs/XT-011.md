# Agent handoff

## Delivered

- Task: XT-011, Define hostile path and manifest fixtures
- From owner: manifest-fixtures-agent
- To owner or reviewer: integration-owner
- Branch: `task/XT-011`
- Worktree: `/Users/bytedance/XnnTransfer/XnnTransfer-XT-011`
- Base SHA: `fe87661dcb2a6101e02390b202adb53180eddc87`
- Head SHA: implementation commit containing this handoff; use
  `git rev-parse HEAD`
- Worktree clean: yes after the task commit
- Owned paths: `protocol/testdata/v1/manifest/**`, `protocol/spec/v1.md`,
  `.agents/handoffs/XT-011.md`, and `.agents/records/XT-011.json`
- Observable behavior: validates 66 deterministic legal and hostile v1
  manifest objects with stable protocol errors and reasons, independent of the
  validator host

## Contracts

- Added or changed: adds protocol-object fixture format version 1, explicit
  fixture comparison profiles, deterministic exact-boundary entry generation,
  checked incremental counters, and path/manifest validation reasons
- Compatibility impact: no wire field, message, limit, security profile, or
  storage behavior changed; fixture comparison profiles are test inputs rather
  than negotiated protocol values
- ADR or protocol reference: preserves ADR 0004 and documents the corpus under
  section 18 of `protocol/spec/v1.md`; no new ADR is required

## Verification evidence

- Command: `python3 protocol/testdata/v1/manifest/validate_vectors.py`
- Result: passed all 66 legal and hostile manifest vector cases
- Command: `python3 protocol/testdata/v1/validate_vectors.py`
- Result: passed all 29 existing framing and negotiation vector cases
- Command: `python3 -m json.tool
  protocol/testdata/v1/manifest/vectors.json`
- Result: passed JSON parsing
- Command: `make verify`
- Result: passed layout and governance checks, all 5 native/CTest tests,
  Flutter formatting with 0 changes, Flutter analysis with 0 issues, and all
  26 Flutter tests
- Skipped gate and reason: none

## Residual risk

- Known limitation: the Windows fixture profile intentionally asserts only
  portable ASCII case collisions; production destination code must apply its
  complete native Unicode and storage policy
- Known limitation: the validator reads fixture JSON and validates protocol
  objects in memory; it does not inspect destination paths, resolve links,
  create files, or prove a storage implementation
- Follow-up task: future native manifest and storage tasks should consume this
  corpus while retaining their own filesystem-boundary and link-refusal tests

## Review focus

- Files or invariants requiring close review: rejection priority and stable
  reasons in `validate_vectors.py`, exact 100,000-entry and 33,554,432-byte
  generated boundaries, target-profile collision behavior, ancestor checks,
  and offer/end checked-summary comparisons

## Acceptance

- Accepted by: pending integration-owner review
- Accepted at: pending integration-owner review
- Follow-up runtime state: `review`
- Integration strategy: `agent.sh integrate` using `git cherry-pick -x`
- Integrated SHA: pending integration
- Verification reference: pending `agent.sh transition XT-011 review`
