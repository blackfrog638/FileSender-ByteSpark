# XT-051 handoff

## Delivered

- Task: XT-051, pinned Linux Secret Service dependency
- From owner: `linux-secret-dependency-agent`
- To owner or reviewer: `integration-owner`
- Branch: `task/XT-051`
- Worktree:
  `/Users/bytedance/XnnTransfer/XnnTransfer-XT-051-corrected`
- Base SHA: `e6d02f2b0b017ebb0872b9e328852a7f305c67a4`
- Head SHA: `dcf78b858b5e74fd313e0a97d75ba5d12580df12`
- Worktree clean: yes before this handoff
- Owned paths: task records, vcpkg manifest, dependency CMake and probes,
  dependency governance tests, libsecret license, CI, ADR 0006, and testing
  documentation
- Observable behavior: Linux manifest builds resolve libsecret 0.21.7 and its
  transitive graph only from the pinned static vcpkg installation; macOS and
  Windows do not install or link libsecret.

## Contracts

- Added or changed: private `XnnDependencies::libsecret` CMake target on Linux;
  exact libsecret source, hashes, license, registry support expression, and
  platform expression are governed.
- Compatibility impact: Linux Flutter entrypoints retain CMake 3.13
  compatibility. No C ABI, wire protocol, persisted schema, or public native
  header changed.
- ADR or protocol reference: amended accepted ADR 0006 with the exact landed
  Linux dependency. Runtime Secret Service behavior remains owned by XT-022.

## Verification evidence

- Command: `make dependency-test`
- Result: passed; 28 positive/negative governance fixtures passed, the pinned
  vcpkg checkout was clean, and the macOS static dependency probe passed.
- Command: `make security-test`
- Result: passed; 14 sanitizer/native tests passed. The C API and protocol
  fuzzers completed 2,542,895 and 825,131 executions without failure.
- Command: `make verify`
- Result: passed; layout, commit policy, dependency, architecture, ABI,
  governance lifecycle, 15 native tests, Flutter analysis, and 28 Flutter
  tests passed.
- Command: `bits-code-guard` review of `HEAD`
- Result: initial findings were fixed; final cross-group review reported no
  P0-P2 findings. Reports:
  `/tmp/XnnTransfer_XT051_postfix_review_20260808/report.html` and
  `/tmp/XnnTransfer_XT051_postfix_review_20260808/report.md`.
- Skipped gate and reason: the gate-generated Linux libsecret runtime link
  probe cannot run on this macOS host. Linux CI must prove the real pinned
  static archive graph and symbol execution before acceptance.

## Residual risk

- Known limitation: local evidence cannot establish that the pinned registry's
  full Linux GLib/libgcrypt static transitive graph links on GitHub's Linux
  image. The generated Linux-only probe is fail-closed and will provide that
  evidence in CI.
- Follow-up task: XT-022 may implement and qualify the Secret Service protected
  store only after this dependency task is accepted with green Linux CI.

## Review focus

- Files or invariants requiring close review: `PKG_CONFIG_LIBDIR` isolation and
  restoration, vcpkg-root path containment, static archive enforcement, CMake
  3.13 compatibility, expanded `find_package` call auditing, full registry
  checkout cleanliness, and the gate-generated Linux runtime link probe.

## Acceptance

- Accepted by: pending
- Accepted at: pending
- Follow-up runtime state: review
- Integration strategy: pending integration-owner decision
- Integrated SHA: pending
- Verification reference: pending Linux CI
