# Agent handoff

## Delivered

- Task: XT-062 Linux secret service qualification
- From owner: `linux-secret-qualification-agent`
- To owner or reviewer: `integration-owner`
- Branch: `task/XT-062`
- Worktree: `/private/tmp/XnnTransfer-XT-062`
- Base SHA: `4116b9f69ea0959a0e830738e2da596bb70f29e7`
- Head SHA: pending review transition
- Worktree clean: yes after the delivery commit
- Owned paths: Linux identity adapter/header/CMake/tests, CI, ADR 0006, ADR
  0009, and testing documentation declared by the task
- Observable behavior: Linux accepts only the current-user, root-owned
  `/usr/bin/gnome-keyring-daemon`, its encrypted libsecret session, and the
  persistent `login` collection. Other Secret Service implementations,
  service-owner changes, locked collections, and unsafe runtime directories
  fail closed.

## Contracts

- Added or changed: removed the caller-supplied Linux backend qualifier,
  registered the exact GNOME Keyring production policy, fixed the persistent
  collection path, bounded D-Bus calls and item enumeration, and added a real
  GNOME Keyring lifecycle CTest and dedicated Ubuntu 24.04 CI job. The
  architecture declaration is an in-place `identity` module refactor; it adds
  no provider, target, dependency, or temporary lease.
- Compatibility impact: the Linux identity factory now has no qualifier
  parameter. There were no production callers; unsupported providers can no
  longer be admitted by a permissive callback. macOS, Windows, the C ABI, wire
  protocol, and persisted XNSP/identity schemas are unchanged.
- ADR or protocol reference: `docs/adr/0006-p1-native-runtime.md` and
  `docs/adr/0009-protected-identity-storage.md`

## Verification evidence

- Command: `make native-test`
- Result: passed; 20/20 native tests on macOS.
- Command: `make security-test`
- Result: passed; 19/19 sanitizer tests, 2,549,387 C API fuzz executions, and
  1,105,957 protocol fuzz executions.
- Command: `tool/harness/desktop_bundle_test.sh`
- Result: passed; macOS Debug and Release bundles loaded the packaged native
  library and passed the Dart bridge tests.
- Command: `make verify`
- Result: passed; all repository, governance, native, Flutter, ABI,
  architecture, dependency, and lifecycle gates.
- Command: stable patch-id comparison of the accepted canonical product
  payload with the corrected-identity replay, excluding task record and
  handoff metadata
- Result: both product payloads equal
  `4098b6cae654c631b761bd27a943346c6e0be2be`.
- Skipped gate and reason: the focused
  `xnn_transfer_linux_secret_service_integration_tests` CTest requires Linux,
  D-Bus, and `/usr/bin/gnome-keyring-daemon`; it cannot run on this macOS
  worktree. The fixed Ubuntu 24.04 CI job must pass before acceptance.

## Residual risk

- Known limitation: GNOME Keyring supplies no trusted monotonic counter, so a
  complete mutually consistent old root and peer snapshot remains
  undetectable. The runtime also cannot detect third-party synchronization of
  the user's encrypted keyring directory; such deployments remain outside the
  qualified profile.
- Follow-up task: XT-032 retains the cross-platform P1 acceptance claim and
  must require the dedicated Linux qualification job.

## Review focus

- Files or invariants requiring close review: verify the executable
  device/inode check cannot be bypassed by another D-Bus owner; verify
  `/org/freedesktop/secrets/collection/login` and encrypted-session checks
  remain fail closed; review all GVariant ownership/signatures and the
  two-process `flock` evidence; confirm the real-service tests do not overclaim
  complete-snapshot rollback detection.

## Acceptance

- Accepted by: pending
- Accepted at: pending
- Follow-up runtime state: pending
- Integration strategy: squash
- Integrated SHA: pending
- Verification reference: pending Ubuntu 24.04 CI
