# Agent handoff

## Delivered

- Task: XT-019, discovery v1 contract
- From owner: `p1-discovery-contract-agent`
- To owner or reviewer: `integration-owner`
- Branch: `task/XT-019`
- Worktree: `/Users/bytedance/XnnTransfer/XnnTransfer-XT-019`
- Base SHA: `710f44f0dd986f1100ebf44cb6aeb4567c78065e`
- Head SHA: implementation commit `cc6a541`; this handoff and review transition
  follow it in the submitted source range
- Worktree clean: yes after handoff and review commits
- Owned paths: discovery v1 protocol specification and fixtures, ADR 0007,
  task record, and handoff
- Observable behavior: a deterministic standard-library oracle accepts 40
  byte-level cases and 14 cache/lifecycle scenarios; no socket or product
  discovery behavior is implemented

## Contracts

- Added or changed: fixed discovery UDP groups and port, a 44-octet v1.0
  header, canonical bounded TLVs, source-derived endpoints, interface-scoped
  random tokens, monotonic sequence/TTL behavior, withdrawals, tombstones,
  interface/wake transitions, and hard rate/memory ceilings
- Compatibility impact: v1.0 rejects unknown envelope versions and critical
  fields; unknown noncritical TLVs are bounded and ignored; no C ABI, transfer
  protocol, persisted schema, or authenticated security profile changed
- ADR or protocol reference: `docs/adr/0007-lan-discovery.md` and
  `protocol/spec/discovery-v1.md`

## Verification evidence

- Command: `python3 protocol/testdata/discovery/v1/validate_vectors.py`
- Result: passed 40 legal/hostile datagram cases and 14 lifecycle cases,
  including truncation, spoofing-relevant metadata, limits, duplicate/conflict
  handling, exact expiry, withdrawal ordering, interface removal, and wake
- Command: `black --check
  protocol/testdata/discovery/v1/validate_vectors.py`
- Result: passed with no formatting drift
- Command: `python3 -m json.tool
  protocol/testdata/discovery/v1/vectors.json`
- Result: parsed successfully
- Command: `make verify`
- Result: governance passed for 40 tasks, lifecycle regression passed, all 8
  CTest targets passed, Flutter analyze passed, and all 26 Flutter tests passed
- Skipped gate and reason: real multicast, interface-monitor, sleep/wake, and
  cross-platform interoperability tests require the XT-020 native runtime and
  are not evidence this protocol-only task can produce

## Residual risk

- Known limitation: multicast group/port collision and LAN source spoofing can
  reduce availability; bounded tombstones suppress only recent replay and do
  not authenticate or permanently remember hostile tokens
- Known limitation: optional display labels, endpoint addresses, timing, and
  service presence remain observable and correlatable on the LAN
- Follow-up task: XT-020 implements the native multicast service, fake-clock
  state tests, platform interface adapters, and shutdown barrier; XT-021 later
  exposes only copied untrusted candidates through the C ABI

## Review focus

- Files or invariants requiring close review: header offsets and exact length
  checks; OS truncation rejection; source address as the only endpoint address;
  no identity/file/trust data; equal sequence never refreshes; 60-second
  tombstones remain bounded; source-bucket tables and candidate state cannot
  grow without limit; wake and interface removal cannot preserve stale peers

## Acceptance

- Accepted by: pending integration-owner review
- Accepted at: pending integration-owner review
- Follow-up runtime state: transition to `review`
- Integration strategy: default squash delivery
- Integrated SHA: pending
- Verification reference: pending review transition
