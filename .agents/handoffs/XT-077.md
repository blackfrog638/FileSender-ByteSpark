# Agent handoff

## Delivered

- Task: XT-077 — Project delivery dashboard
- From owner: blackfrog638
- To owner or reviewer: integration-owner
- Branch: task/XT-077
- Worktree: /Users/bytedance/XnnTransfer/XnnTransfer-XT-077
- Base SHA: b3e64de5b3734dba55c7b3bacdb39a2df769b135
- Head SHA: a59b0b9 (feat delivery d925b85 + reserved-id robustness fix)
- Worktree clean: yes
- Owned paths: `.agents/backlog.yaml`, `.agents/manifest.yaml`,
  `.agents/plans/DP-PROJECT-DASHBOARD.json`, `.agents/records/XT-077.json`,
  `.agents/tasks/XT-077-project-delivery-dashboard.md`,
  `.agents/handoffs/XT-077.md`, `tool/harness/agent.sh`,
  `tool/harness/dashboard.py`, `tool/harness/dashboard_test.py`,
  `tool/harness/governance_test.sh`, `Makefile`, `.gitignore`
- Observable behavior: `tool/harness/agent.sh dashboard [--output PATH]`
  (equivalently `python3 -B tool/harness/dashboard.py`) regenerates a
  self-contained HTML dashboard at the git-ignored path
  `build/dashboard/index.html`. The page renders four read-only sections:
  the task runtime-state swimlanes (resolved exactly as `agent.sh list`), the
  delivery-plan requirement tables (derived via `delivery_plan`), the roadmap
  milestones, and the manifest verification gate registry. Tasks whose live
  task-branch state is ahead of the durable record carry a `branch` badge.

## Contracts

- Added or changed: none. The dashboard is a new read-only harness tool plus a
  new `dashboard_test` verification gate wired through the manifest command
  registry, `Makefile`, `governance_test.sh`, and an `agent.sh dashboard`
  subcommand. No C ABI, wire protocol, persisted schema, security profile, or
  shared cross-workstream type changes.
- Compatibility impact: none. Existing gates, commands, and subcommands are
  unchanged; only additive entries were introduced.
- ADR or protocol reference: not required. The change introduces no new or
  changed public cross-workstream contract; it is confined to the harness
  workstream and derives exclusively from existing tracked sources of truth.

## Verification evidence

- Command: `make dashboard-test`
- Result: pass — 8 tests OK. Asserts the aggregation matches a reference reimplementation of `agent.sh list` (including an active-branch divergence case and terminal-durable precedence), that requirement state matches `delivery_plan.py status`, that a draft plan reserving an unregistered future task id degrades to a rendered `planned`/`reserved` state instead of crashing (the exact real-repository condition caused by `DP-FUTURE-TDD`), that milestones and gates parse from their sources, that an untrusted `<script>` title is HTML-escaped, and that the artifact is written.
- Command: `make governance-test`
- Result: pass (exit 0) — trusted gates, defect proof, task conflicts, delivery plan, `dashboard_test`, GitHub CI, and the full lifecycle integration/acceptance simulation all green with the new gate wired in.
- Command: `make verify`
- Result: pass (exit 0) — Native 27/27, `flutter analyze` clean, Flutter 53 tests pass, "All repository verification gates passed."
- Skipped gate and reason: none.

## Residual risk

- Known limitation: the dashboard is a static snapshot generated on demand; it
  is not a live server and does not auto-refresh. Regenerate after state
  changes. This is intentional — the artifact stores no authoritative state.
- Known limitation: roadmap milestone grouping relies on `docs/roadmap.md`
  Markdown headings and `- [ ]` checkboxes plus optional
  `<!-- roadmap-id: ... -->` markers; roadmap prose that does not follow that
  shape is not rendered as a milestone. This is a presentation concern only.
- Follow-up task: none required.

## Review focus

- Files or invariants requiring close review:
  - `tool/harness/dashboard.py` `_resolve_runtime_state`: must stay
    byte-for-byte faithful to `agent.sh list_tasks` (durable wins only when
    terminal; otherwise the task-branch record is authoritative; divergence is
    surfaced, never silently reconciled).
  - `_load_requirements` / `_plan_task_state`: must keep reusing
    `delivery_plan._record_state`/`_scheduled_state`/`_requirement_state`
    verbatim for registered tasks so the dashboard can never disagree with
    `delivery_plan.py status`. A task id a draft plan reserves before its
    record exists degrades to the `reserved` sentinel (rendered `planned` at
    the requirement level) instead of crashing; a record that exists but is
    structurally broken still raises.
  - `_esc`/`render_html`: every piece of task, backlog, roadmap, and manifest
    text is routed through `html.escape(..., quote=True)`; confirm no
    interpolation bypasses it.
  - Artifact boundary: `.gitignore` excludes `build/dashboard/`; confirm the
    generator never writes into tracked paths.

## Acceptance

- Accepted by:
- Accepted at:
- Follow-up runtime state:
- Integration strategy:
- Integrated SHA:
- Verification reference:

The submitted handoff may leave acceptance fields pending while the task is in
`review`. Final acceptance is written to `.agents/records/XT-077.json` by
`agent.sh accept`; do not edit a submitted task handoff solely to duplicate the
durable acceptance record.
