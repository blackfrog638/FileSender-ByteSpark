# Agent handoff

## Delivered

- Task: XT-069, authenticated connection runtime
- From owner: `authenticated-connection-agent`
- To owner or reviewer: `integration-owner`
- Branch: `task/XT-069`
- Worktree: `/Users/bytedance/XnnTransfer/XnnTransfer-XT-069`
- Base SHA: `f814c42e20f14583e8aca095eeca70707a5cd595`
- Head SHA: pending review transition
- Worktree clean: yes after the delivery commit
- Owned paths: task architecture metadata and handoff; native TLS and session
  public interfaces, implementations, CMake targets, and tests
- Observable behavior: one dual-stack Asio listener now accepts real TLS 1.3
  sockets, rejects missing, duplicate, multiple, and unknown ALPN offers,
  dispatches pairing and established modes as distinct typed capabilities,
  drives bounded XNNP pairing to durable trust, and exposes authenticated
  established byte streams with serialized access and a callback-safe stop
  barrier; external shutdown now stops and joins the Asio executor before
  closing owned resources without concurrency, while reentrant shutdown closes
  resources on the executor. Post-review corrections now reserve global and
  per-source admission before TLS work, enforce the protocol's five-second TLS
  ceiling and ten-second frame-assembly deadline, close terminal pairing
  transports at most one second after the terminal event, give a completed
  terminal write at most 250 milliseconds to reach its peer without extending
  that deadline, bind typed TLS capabilities to their originating context and
  SSL object, and deliver accepted established-I/O callbacks exactly once on
  ordinary close. The runtime also preserves TLS plaintext returned together
  with a terminal read error and uses exclusive listener binding on Windows.

## Contracts

- Added or changed: added `AuthenticatedConnectionRuntime`,
  `AuthenticatedEstablishedConnection`, canonical network endpoints, shared
  registered ALPN constants, typed accepted-server TLS capabilities, and
  server-side active exact-pin resolution; existing client channel factories
  remain source compatible
- Compatibility impact: no C ABI, wire encoding, persisted schema, cipher,
  group, exporter, or security-profile decision changed; the server now
  implements the accepted same-port ALPN and active-pin policy and rejects
  expired identity certificates
- Architecture impact: metadata declares an in-place `refactor` of the
  existing canonical `tls` and `session` modules, with no new provider,
  supersession claim, temporary lease, or dependency direction
- ADR or protocol reference: `protocol/spec/pairing-v1.md` and
  `docs/adr/0002-pairing-and-transport-security.md`

## Verification evidence

- Command: focused dev and sanitizer CTests for
  `xnn_transfer_tls_provider_tests`, `xnn_transfer_pairing_session_tests`,
  `xnn_transfer_session_authority_tests`, and
  `xnn_transfer_connection_runtime_tests`
- Result: passed 4/4 under both the dev and ASan/UBSan presets, including
  malformed ASN.1 certificate time, connection-bound typed capabilities,
  pre-TLS global/per-source admission, the five-second TLS hard limit, partial
  frame expiry from the first header octet, terminal flush timing, ordinary
  close callback completion, and blocking user callbacks
- Command: run the ASan/UBSan authenticated runtime test 20 consecutive times
  after adding the bounded terminal flush
- Result: passed 20/20; local rejection remained `PAIRING_LOCAL_REJECT` and
  the confirming peer observed `PAIRING_AUTHENTICATED_REJECT`
- Command: GitHub Actions delivery run
  `https://github.com/blackfrog638/XnnTransfer/actions/runs/31397722206`
- Result: exposed a Windows-only 120-second timeout in
  `xnn_transfer_connection_runtime_tests`; build, ABI, and all preceding
  Windows tests passed. The runtime stop barrier was changed to stop the
  `io_context` after synchronous resource closure, and each runtime scenario
  now emits an immediate progress marker for platform diagnostics.
- Command: corrected GitHub Actions delivery run
  `https://github.com/blackfrog638/XnnTransfer/actions/runs/31403213281`
- Result: proved the first correction was insufficient: Windows again timed
  out after printing only the first scenario start marker, with no bounded
  network assertion failure. External stop no longer posts cleanup and waits
  for the IOCP cancellation queue; it stops and joins the executor first, then
  closes resources without concurrent executor access. Synchronous runtime
  commands also expire after five seconds and invalidate queued work.
- Command: run the corrected authenticated runtime test 20 consecutive times
  and assert that shutdown completes within two seconds
- Result: passed 20/20; focused CTest completed in 2.08 seconds and all detailed
  stages reached `runtime pair stopped`
- Command: race four external `Stop()` calls and repeat the complete runtime
  suite 50 times under dev plus 20 times under ASan/UBSan
- Result: the new test first reproduced a duplicate-join exception in the
  `running=false`/`thread_exited=false` window; the corrected lock-protected
  `joinable()` path then passed 50/50 dev and 20/20 sanitizer runs
- Command: bits-code-guard grouped review of
  `1c3b2378b45f0f3f43b375d4752e31319ff34088..403919652499d6bcb013c70d029d9cb93159340c`
- Result: corrected all nine findings: pre-TLS admission, the configurable TLS
  timeout bypass, malformed certificate time acceptance, accept-handler
  self-ownership, incomplete-frame deadline bypass, late terminal flush
  timing, ambiguous timed-out runtime commands, dropped ordinary-close read
  callbacks, and transferable typed TLS capabilities. The generated report is
  `/tmp/XnnTransfer_xt069_review_20260811/report.html`.
- Command: rebase the task onto integration
  `0f3113346bad910b8eafe96315bdf7b63466d320` after relevant harness and
  repository gate changes
- Result: rebase completed without conflicts; the task base and handoff were
  updated, and all required gates below were repeated on the rebased source
- Command: repeat the final grouped review and rebase onto integration
  `80f6b4accffafaa61a1fd19c632ade6961f913d2`
- Result: the review found four P1 and two P2 issues across ALPN admission,
  process-wide limits, callback scheduling, frame-prefix validation, callback
  completion, and typed capability freshness; the task returned to
  `in_progress` for correction
- Command: repeat the corrected dev
  `xnn_transfer_connection_runtime_tests` five times
- Result: passed 5/5, including concurrent/reentrant/repeated stop,
  callback-ordering, pre-TLS limits, frame deadline, and terminal flush cases
- Command: `make native-test`
- Result: passed 28/28 native tests after the review corrections
- Command: `make security-test`
- Result: passed 27/27 ASan/UBSan tests after the review corrections; C API
  fuzz completed 2,144,643 executions and protocol fuzz completed 1,046,985
  executions without a crash
- Command: `make verify`
- Result: passed repository layout, commit policy, pinned dependencies,
  architecture, ABI 4/4, governance lifecycle, native 28/28, Flutter analyze,
  and Flutter 28/28 on the review-correction source
- Command: rebase the complete task range onto integration
  `cada8ddf299ef36fa33642342912e58fef35ee97`
- Result: completed without conflicts after the accepted TDD governance
  changes; task record, handoff base, and all final gates were refreshed
- Command: rebase the reviewed task range onto integration
  `bf611967420b70ac0069825e1649a79c3cfec9b9`
- Result: completed without conflicts after XT-079 acceptance; the product
  payload was unchanged, and the final trusted gates were repeated
- Command: ten successive bits-code-guard Group 3 reviews of the complete
  runtime and established-connection payload
- Result: corrected shutdown command cycles, callback-capacity accounting,
  startup rollback ownership, ordinary-close handoff, both callback/worker
  Stop orderings, and pairing/event allocation-failure paths; the final review
  reported zero P0-P2 findings in
  `/tmp/XnnTransfer_xt069_rereview10_20260811/group/group_3.jsonl`
- Command: focused dev and ASan/UBSan CTests plus repeated authenticated runtime
  execution after the final review corrections
- Result: passed 4/4 in both presets, 10/10 dev repetitions, and 15 consecutive
  sanitizer repetitions after one non-reproducing rejection-timing mismatch;
  the deterministic callback-first/worker-first Stop cases both complete
  without circular waits
- Command: final `make native-test`
- Result: passed 28/28 native tests
- Command: final `make security-test`
- Result: passed 27/27 ASan/UBSan tests; C API fuzz completed 2,063,173
  executions and protocol fuzz completed 762,512 executions without a crash
- Command: final `make verify`
- Result: passed repository layout, commit policy, pinned dependencies,
  architecture, ABI 4/4, governance lifecycle, native 28/28, Flutter analyze,
  and all Flutter tests on the final zero-finding source
- Command: GitHub Actions delivery run
  `https://github.com/blackfrog638/XnnTransfer/actions/runs/31442825611`
- Result: Native Windows timed out during retained established-handle teardown
  after the explicit runtime stop returned; the sanitizer job independently
  reported an invalid Asio timer-service vptr when a retained TLS stream was
  destroyed after its runtime `io_context`
- Command: rebase the integration fix onto failed delivery
  `0b6f20b4856a98bcda83d20f845828542afa9a86`
- Result: retained established connections now keep the runtime executor and
  its services alive through TLS stream destruction; the regression test
  explicitly releases both public runtimes before releasing retained handles
- Command: integration-fix `make native-test`
- Result: passed 28/28 native tests, including bounded retained-handle teardown
- Command: integration-fix `make security-test`
- Result: passed 27/27 ASan/UBSan tests without the invalid-vptr report; C API
  fuzz completed 1,971,476 executions and protocol fuzz completed 763,484
  executions without a crash
- Command: bits-code-guard review of the integration-fix working-tree diff
- Result: found no P0-P2 defects across executor ownership, TLS stream
  destruction order, shutdown cycles, and retained-handle teardown; report:
  `/tmp/XnnTransfer_xt069_integration_fix_review_20260811/report.html`
- Command: integration-fix `make verify`
- Result: passed repository layout, commit policy, pinned dependencies,
  architecture, ABI 4/4, governance lifecycle, native 28/28, Flutter analyze,
  and all Flutter tests
- Command: archive the failed delivery, reopen base, reviewed source, and
  replacement delivery under `refs/archive/XT-069/20260811-*`
- Result: preserved `0b6f20b`, `ee9802b`, `9ad4c7a`, and `b9d30d8` before
  removing the stale task worktree and branch
- Command: reclaim XT-069 from accepted base `bf61196`, migrate the aggregate
  replacement payload while excluding the old lifecycle record, and compare
  stable patch IDs
- Result: the reclaimed payload and archived replacement both produced
  `44576d19a8d5aab90cd825faa0dab79016f01562`
- Command: initial reclaimed `make native-test`
- Result: configuration stopped before compilation because the new worktree
  did not contain the pinned vcpkg checkout
- Command: `make vcpkg-bootstrap`, then repeat `make native-test`,
  `make security-test`, and `make verify`
- Result: pinned vcpkg was restored; native passed 28/28, sanitizers passed
  27/27, C API and protocol fuzz completed 1,949,530 and 1,130,375 executions,
  and all repository verification gates passed
- Command: stale-base check against accepted `harness` at `268fd25`, rebase
  onto that revision, update `base_sha`, and repeat the stable patch-ID check
- Result: the governance change required a rebase; it completed without
  conflicts, the stale-base gate passed, and the product patch ID remained
  `44576d19a8d5aab90cd825faa0dab79016f01562`
- Command: final-base `make native-test`, `make security-test`, and
  `make verify`
- Result: native passed 28/28; sanitizers passed 27/27; C API and protocol
  fuzz completed 1,897,750 and 1,129,989 executions; repository layout,
  commit policy, dependencies, architecture, ABI 4/4, governance lifecycle,
  native 28/28, Flutter analyze, and all 53 Flutter tests passed
- Command: GitHub Actions delivery run
  `https://github.com/blackfrog638/XnnTransfer/actions/runs/31457594489`
- Result: Flutter and harness contracts passed, but the delivery failed three
  gates. macOS received an authenticated rejection frame together with a TLS
  terminal error and discarded the frame as `PAIRING_INTERNAL_FAILURE`.
  Windows allowed the occupied listener port to be rebound because
  `SO_REUSEADDR` is not exclusive there. Commit policy also could not compare
  the force-updated candidate with its non-ancestor predecessor `b9d30d8`.
  The harness deleted `ci/XT-069` after the failed run.
- Command: archive failed delivery `cd66f1095a120292f767f9d62bac0a30c3e07723`
  under `refs/archive/XT-069/20260811-reclaimed-delivery-ci-failure`, restore
  canonical `harness` to accepted `268fd25`, and return XT-069 to
  `in_progress`
- Result: the failed delivery remains reachable, the integration worktree
  exactly matches `origin/harness`, and the new reviewed range contains the
  required integration fixes instead of reusing failed provenance
- Command: process non-empty TLS reads before their accompanying terminal
  error, configure Windows listeners with `SO_EXCLUSIVEADDRUSE`, and repeat
  `xnn_transfer_connection_runtime_tests` 20 consecutive times
- Result: passed 20/20, including authenticated rejection, occupied-port
  startup rollback, concurrent stop, and retained established-handle teardown
- Command: post-fix `make architecture-test`, `make native-test`,
  `make security-test`, and `make verify`
- Result: architecture passed; native passed 28/28; sanitizers passed 27/27;
  C API and protocol fuzz completed 1,969,104 and 856,747 executions; all
  repository gates, Flutter analysis, and 53 Flutter tests passed
- Command: locally accept integrated delivery
  `bec4a21a5729859afd9dbc666359dda14ef54307`
- Result: the acceptance gate reproduced a zero-byte TLS truncation after the
  rejecting peer had completed its authenticated rejection write; the
  confirming peer reported `PAIRING_INTERNAL_FAILURE`. No candidate ref was
  pushed, so protected `origin/harness` remained unchanged.
- Command: archive failed delivery `bec4a21a5729859afd9dbc666359dda14ef54307`
  under `refs/archive/XT-069/20260811-reclaimed-delivery-local-flake`, restore
  canonical `harness` to accepted `268fd25`, and return XT-069 to
  `in_progress`
- Result: the failed delivery remains reachable, the integration worktree
  again matches `origin/harness`, and the task contains a fresh integration-fix
  source range
- Command: replace the immediate post-write transport close with a bounded
  250-millisecond grace, preserve the original one-second terminal hard
  deadline, and run 20 authenticated-rejection iterations in each runtime test
- Result: the confirming peer consistently received
  `PAIRING_AUTHENTICATED_REJECT`; each completed iteration released and reset
  the test-only process admission owner before the next iteration without
  changing production admission behavior
- Command: repeat `xnn_transfer_connection_runtime_tests` 20 consecutive times
- Result: passed 20/20, exercising 400 authenticated rejections plus the full
  runtime shutdown, deadline, retained-handle, and terminal-flush suite
- Command: final integration-fix `make architecture-test`, `make native-test`,
  `make security-test`, and `make verify`
- Result: architecture passed; native passed 28/28; sanitizers passed 27/27;
  C API and protocol fuzz completed 1,896,185 and 1,145,430 executions; all
  repository layout, commit policy, dependency, ABI 4/4, governance lifecycle,
  Flutter analysis, and 53 Flutter tests passed
- Command: archive the pre-governance source under
  `refs/archive/XT-069/20260811-pre-tdd-governance-rebase`, then rebase the
  complete task range onto integration `8b87bfc`
- Result: completed without conflicts after the XT-081 global TDD-governance
  change; XT-069's product payload is unchanged, and its `base_sha` now names
  the current integration head
- Command: after XT-081 completed dual-SHA remote acceptance, archive the
  pending-base source under
  `refs/archive/XT-069/20260811-pre-xt081-acceptance-rebase` and rebase onto
  accepted `f814c42`
- Result: completed without conflicts; XT-069 now starts from the protected
  acceptance SHA rather than XT-081's intermediate delivery SHA
- Command: GitHub Actions delivery run
  `https://github.com/blackfrog638/XnnTransfer/actions/runs/31469920867`
- Result: every gate passed except Native macOS. Windows proved exclusive
  listener binding and the complete runtime suite passed there. macOS admitted
  the first and third raw sockets before the second across two independent
  runtime executors, exposing that the process-wide admission test incorrectly
  assumed `connect()` call order was accept order.
- Command: archive failed delivery `2d3a3885dd7d2decefd6c79643eea664dfaad68f`
  under
  `refs/archive/XT-069/20260811-reclaimed-delivery-macos-admission-failure`,
  restore canonical `harness` to accepted `f814c42`, and return XT-069 to
  `in_progress`
- Result: the failed delivery remains reachable, protected `origin/harness`
  was never changed, and the candidate ref was deleted by the harness
- Command: observe the shared admission controller while connecting the first
  two raw sockets, wait until each retained lease is recorded, then connect
  the third socket and explicitly retire the test observer
- Result: the test now verifies the process-wide per-source ceiling instead of
  executor scheduling order; production admission behavior is unchanged
- Command: repeat `xnn_transfer_connection_runtime_tests` 20 consecutive times
- Result: passed 20/20, including 400 authenticated rejections and 20 stable
  process-wide pre-TLS admission scenarios
- Skipped gate and reason: none; the corrected Windows and Linux runtime
  behavior remains subject to a fresh required remote CI run before acceptance

## Residual risk

- Known limitation: the established capability is an authenticated bounded
  byte stream only; this task does not publish transfer frames, execute file
  workers, or claim that a file can traverse the network
- Follow-up task: XT-070 composes the authenticated stream with one-file
  transfer framing, publication ordering, storage workers, and cancellation

## Review focus

- Files or invariants requiring close review: ClientHello ALPN presence and
  single-mode validation; active exact-pin resolution before established
  dispatch; two-stage pre/post-TLS admission without a new public controller
  API; context/SSL/ALPN ownership of verified capabilities; XNNP frame and
  terminal timers; runtime-command linearization; ordinary-close callback
  completion; external, reentrant, and concurrent stop behavior; absence of
  callbacks after the stop barrier

## Acceptance

- Accepted by: pending integration-owner review and remote CI
- Accepted at: pending integration-owner review and remote CI
- Follow-up runtime state: transition to `review`
- Integration strategy: default squash delivery
- Integrated SHA: pending
- Verification reference: pending GitHub Actions runs for the delivery and
  acceptance SHAs
