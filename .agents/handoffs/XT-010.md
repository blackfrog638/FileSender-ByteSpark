# Agent handoff

## Delivered

- Task: XT-010, Independently review pairing security ADR
- From owner: `security-review-agent`
- To owner or reviewer: `integration-owner` and the follow-up profile-fix owner
- Branch: `task/XT-010`
- Worktree: `/Users/bytedance/XnnTransfer/XnnTransfer-XT-010`
- Base SHA: `e070b5f8072eb2ae18a5f5b8c9939eae1aa33b44`
- Head SHA: pending the final third-review commit; use the task record after
  the blocked transition
- Worktree clean: yes after the final review and blocked-state commits
- Owned paths: `docs/adr/0002-pairing-and-transport-security.md`,
  `protocol/security/**`, this task's record, and this handoff
- Observable behavior: no runtime behavior changed; the third independent
  review closes BR-03 but finds BR-04, where the proposed public-key predicate
  accepts low-order and mixed-order points and permits an identity-key
  possession forgery

## Contracts

- Added or changed: updated
  `protocol/security/XT-010-independent-review.md` with RFC TEST 1/2/3
  provenance, independent 17-vector recalculation, the exact 16-output drift,
  all 34 rejection outcomes, subgroup arithmetic, cross-backend evidence, and
  BR-04 remediation requirements
- Compatibility impact: no C ABI, wire protocol, persisted schema, accepted
  security profile, or implementation contract changed
- ADR or protocol reference: ADR 0002 remains `proposed`; RFC point
  decompression alone does not satisfy its private-key-possession requirement
- Architecture and roadmap impact: no task-local update; both files remain
  integration-owned and must be synchronized only after a later independent
  review accepts a corrected profile

## Verification evidence

- Command:
  `python3 protocol/testdata/security/v1/validate_vectors.py`
- Result: passed all 17 positive and 34 negative checked-in vectors
- Command: fetch RFC 8032 section 7.1, extract TEST 1/2/3 seeds and keys, and
  derive each public key with OpenSSL 3.6.3
- Result: all three OpenSSL-derived public keys matched the RFC and fixture
  bytes exactly
- Command: independent inline Ruby canonical encoder, SHA-256, OpenSSL HMAC,
  HKDF Expand, SAS mapper, device-ID derivation, and Edwards25519 arithmetic,
  without importing or invoking the Python validator
- Result: matched 17 of 17 positive outputs; confirmed exactly 16 post-XT-014
  drifts; proved TEST 1/2/3 are distinct non-identity prime-subgroup points;
  and matched both valid-key device identifiers
- Command: structured Ruby comparison of pre-XT-014 and current manifests
- Result: all 32 prior rejection codes remained stable and the two new cases
  were exactly `INVALID_PUBLIC_KEY` and `NON_CANONICAL_ENCODING`
- Command: independent Edwards25519 scalar multiplication over identity,
  order-2, order-4, and TEST-1-plus-order-2 points
- Result: the fixture predicate accepted all tested low/mixed-order encodings;
  the mixed-order point was not small-order, proving a blacklist is
  insufficient
- Command: OpenSSL 3.6.3 `pkey -pubcheck`/`pkeyutl -verify`, Node/OpenSSL, and
  Apple CryptoKit over identity key `01 00..00` with `R=B,S=1`
- Result: OpenSSL labeled the hostile keys valid and all three verification
  APIs accepted the no-private-key identity signature; Node accepted it for
  three distinct messages
- Command: `make verify`
- Result: passed repository layout, 14-task governance and lifecycle, all 7
  CTest targets, Flutter formatting/analyze, and all 26 Flutter tests
- Skipped gate and reason: none

## Residual risk

- Known limitation: release-blocking BR-04 remains because ADR 0002 and its
  fixture oracle require only canonical point decompression, not a non-identity
  prime-order public key
- Known limitation: affected maintained backends accept a universal identity
  signature while stricter implementations reject low-order and
  non-main-subgroup points, so both authentication and cross-platform
  agreement are undefined
- Known implementation gates: no live TLS/exporter or Ed25519 conformance, no
  pairing wire state machine or ALPN/profile registration, no secure-storage
  fault evidence, and no completed negative-matrix integration/platform/fuzz
  coverage
- Follow-up task: specify and validate non-identity prime-order Ed25519 public
  keys before transcript, device-ID, rotation, pin, or TLS trust use; add
  identity, low-order, mixed-order, and three-platform TLS negatives; then
  return XT-010 to independent review

## Review focus

- Files or invariants requiring close review:
  `protocol/security/XT-010-independent-review.md`, BR-04, RFC 8032 sections
  5.1.3/5.1.5/5.1.7/8.8, SEC-04, P-13, exact pinning, and the required
  `[L]A = identity` plus non-identity key rule

## Acceptance

- Accepted by: not accepted; BR-04 remains
- Accepted at: not applicable
- Follow-up runtime state: `blocked`
- Integration strategy: none while blocked
- Integrated SHA: not applicable
- Verification reference: local third-review verification; final artifact SHA
  is the parent of the blocked-state transition commit
