# Agent handoff

## Delivered

- Task: XT-046, pinned P1 dependency bootstrap
- From owner: `p1-dependency-agent`
- To owner or reviewer: `integration-owner`
- Branch: `task/XT-046`
- Worktree: `/Users/bytedance/XnnTransfer/XnnTransfer-XT-046`
- Base SHA: `a18a1ec`
- Head SHA: implementation commits `af92fae` and `835e7e3`; this handoff and
  review transition follow them in the submitted source range
- Worktree clean: yes after this handoff commit
- Owned paths: task catalogue/records, dependency manifests and overlays,
  CMake/toolchain entrypoints, CI, desktop build entrypoints, licenses, and
  ADR/architecture/testing documentation listed by the task
- Observable behavior: native and Flutter desktop CMake entrypoints require
  vcpkg commit `17f35ad2418007a895ced8a4cece4ab34068a58d`,
  resolve static Asio 1.38.2, OpenSSL 3.5.7, and utf8proc 2.11.3, and run an
  exact-version compiled/linked probe
- Review correction: Linux and Windows Flutter roots explicitly import the
  pinned dependency targets, with a negative fixture rejecting future bypass

## Contracts

- Added or changed: ADR 0006 now selects utf8proc 2.11.3 for complete native
  Unicode validation and records the landed dependency provenance
- Compatibility impact: existing project dynamic library and C ABI are
  unchanged; build hosts now require the pinned vcpkg bootstrap plus Perl and
  pkg-config
- ADR or protocol reference: `docs/adr/0006-p1-native-runtime.md`; no wire or
  C ABI contract changed

## Verification evidence

- Command: `make dependency-test`
- Result: seven negative provenance fixtures passed; clean static triplet builds
  installed all dependencies, the exact-version probe linked and ran, and the
  installed graph contained no `.dll`, `.dylib`, or `.so`
- Command: `make native-test`
- Result: all 13 native/ABI/protocol/dependency tests passed
- Command: `make security-test`
- Result: all 12 ASan/UBSan tests passed; C API and protocol fuzzers completed
  about 2.54 million and 0.83 million iterations without failure
- Command: `make verify`
- Result: dependency provenance, architecture, ABI, governance, all 13 native
  tests, Flutter analysis, and all 26 Flutter tests passed
- Command: `make doctor`
- Result: the complete local toolchain, including the exact vcpkg checkout,
  Perl, and pkg-config, is available
- Remote failure: CI run `31165482575` proved a cold `actions/cache` miss left
  `VCPKG_DEFAULT_BINARY_CACHE` absent, so vcpkg rejected all three desktop
  package builds before dependency compilation
- Integration correction: commit `b4f2667` makes the shared bootstrap create
  the configured cache directory and adds a cache-miss regression to
  `make dependency-test`
- Remote partial success: CI run `31166663906` passed eight of nine jobs,
  including all desktop bundles, Linux/macOS native, and security; its
  `windows-latest` native job linked MinGW objects against MSVC vcpkg archives
- Toolchain correction: the native Windows matrix now uses `windows-2022` and
  the explicit `Visual Studio 17 2022` x64 generator, matching the successful
  Windows desktop build instead of relying on PATH compiler discovery
- Skipped gate and reason: Windows and Linux overlay builds and all three
  packaged Flutter bundles require remote CI and remain pending at handoff

## Residual risk

- Known limitation: local evidence covers macOS arm64; MSVC, Linux, Windows
  OpenSSL install layout, universal macOS, and Flutter packaging are exercised
  by the new CI matrix but cannot be executed on this workstation
- Follow-up task: XT-020 consumes Asio and utf8proc only after this task passes
  all remote platform jobs and is accepted

## Review focus

- Files or invariants requiring close review: release archive URLs and hashes,
  OpenSSL overlay delta from pinned vcpkg, static/CRT triplets, pre-`project()`
  toolchain selection, CI cache identity, system OpenSSL rejection, and the
  distinction between dependency availability and unimplemented product
  behavior

## Acceptance

- Accepted by: pending integration-owner review
- Accepted at: pending integration-owner review
- Follow-up runtime state: transition to `review`
- Integration strategy: default squash delivery
- Integrated SHA: pending
- Verification reference: pending remote CI
