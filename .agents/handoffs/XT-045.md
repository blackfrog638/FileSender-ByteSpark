# Agent handoff

## Delivered

- Task: XT-045, Windows ABI DLL search path
- From owner: `abi-dll-agent`
- To owner or reviewer: `integration-owner`
- Branch: `task/XT-045`
- Worktree: `/Users/bytedance/XnnTransfer/XnnTransfer-XT-045`
- Base SHA: `90ee43f`
- Head SHA: implementation commit `cb44e11` plus base-provenance commit
  `4b07b7d`; this handoff and review transition follow them
- Worktree clean: yes after this handoff commit
- Owned paths: `.agents/**` and `native/tests/abi/**`
- Observable behavior: Windows ABI runtime tests receive the built core DLL
  directory, and the export probe registers valid dependency directories with
  Python's secure DLL loader

## Contracts

- Added or changed: Windows-only test-process DLL search setup
- Compatibility impact: legacy-client calls and required-export checks are
  unchanged; Linux and macOS loader behavior is unchanged
- ADR or protocol reference: enforces ABI v1 evidence required by ADR 0001 and
  ADR 0003 without changing either contract

## Verification evidence

- Evidence: GitHub Actions run 31159543576
- Result: Windows current and frozen layout tests passed; the bounded failures
  isolated legacy-client DLL lookup and `ctypes.CDLL` dependency resolution
- Command: `make abi-compat-test`
- Result: all 5 configure-time verdicts and all 4 runtime ABI tests passed
- Command: `python3 -m py_compile native/tests/abi/export_test.py`
- Result: passed without leaving bytecode artifacts
- Command: `make verify`
- Result: architecture and governance gates, all 12 native tests, Flutter
  analysis, and all 26 Flutter tests passed
- Skipped gate and reason: Windows behavior requires the replacement remote CI
  run and remains pending at handoff

## Residual risk

- Known limitation: this workstation cannot execute Windows loader semantics;
  acceptance requires both Windows legacy-client and export tests to pass
- Follow-up task: none if the replacement three-platform native matrix passes

## Review focus

- Files or invariants requiring close review: CTest
  `ENVIRONMENT_MODIFICATION`, generator-expanded core target directory, PATH
  normalization and deduplication, `os.add_dll_directory` handle lifetime, and
  unchanged non-Windows branch

## Acceptance

- Accepted by: pending integration-owner review
- Accepted at: pending integration-owner review
- Follow-up runtime state: transition to `review`
- Integration strategy: default squash delivery
- Integrated SHA: pending
- Verification reference: pending review transition
