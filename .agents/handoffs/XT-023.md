# Agent handoff

## Delivered

- Task: XT-023
- From owner: tls-provider-agent
- To owner or reviewer: integration-owner
- Branch: `task/XT-023`
- Worktree: `/Users/bytedance/XnnTransfer/XnnTransfer-XT-023-corrected`
- Base SHA: `b8ebe5dd2e4d60ce46bde4770cb781df4e80f3d5`
- Head SHA: `b8529530104ed98446d035886103a35885ac0816`
- Worktree clean: Yes at the product delivery commit; this handoff follows it.
- Owned paths: `native/include/xnn_transfer/core/security/tls/**`,
  `native/src/security/tls/**`, and `native/tests/security/tls/**`
- Observable behavior: The canonical `xnn_transfer_tls` target configures
  OpenSSL for TLS 1.3 only, the accepted suites, X25519, Ed25519, strict ALPN,
  no tickets/cache/early data, one self-signed identity certificate, subgroup
  validation before possession verification, exact optional pinning, and
  typed pairing/confirmation/transport exporters. Native code also implements
  and verifies the accepted canonical security-profile primitives. Review
  hardening makes the verified-connection capability move-only, documents its
  non-owning lifetime, and revalidates the live peer key before every exporter
  operation so an `SSL*` reused for another peer cannot reuse stale authority.

## Contracts

- Added or changed: Added the C++ TLS provider and profile APIs under
  `xnn_transfer/core/security/tls/`; implemented XT-022's
  `PeerPublicKeyValidator` contract. No C ABI, wire format, persisted schema,
  or accepted security-profile value changed.
- Compatibility impact: The provider requires an exact non-empty ALPN token
  from its caller and fails closed on any negotiated-profile mismatch.
- ADR or protocol reference: `docs/adr/0002-pairing-and-transport-security.md`
  and `docs/adr/0006-p1-native-runtime.md`

## Verification evidence

- Command: Focused build plus CTest for
  `xnn_transfer_ed25519_public_key_tests`,
  `xnn_transfer_security_profile_tests`, and
  `xnn_transfer_tls_provider_tests`
- Result: Passed 3/3 focused tests; native profile coverage matched all 17
  positive and 37 negative accepted vectors.
- Command: `make architecture-test`
- Result: Passed 17 architecture tests; canonical TLS placeholder replacement
  and `tls -> identity` dependency were accepted.
- Command: `make native-test`
- Result: Passed 20/20 native tests.
- Command: `make security-test`
- Result: Passed 19/19 ASan/UBSan tests after review hardening; C API fuzzing
  completed 2,585,954 executions and protocol fuzzing completed 853,675
  executions without a crash.
- Command: Focused build plus CTest for
  `xnn_transfer_ed25519_public_key_tests`,
  `xnn_transfer_security_profile_tests`, and
  `xnn_transfer_tls_provider_tests` after review hardening
- Result: Passed 3/3, including move-only capability checks and a live
  `SSL_clear`/reconnect regression that rejects the old capability after the
  same `SSL*` connects to a different identity.
- Command: GitHub Actions run `31237051498`
- Result: macOS and Linux native jobs passed. The Windows native job compiled
  the production TLS library, then rejected the test fixture because Win32's
  `max` macro expanded `numeric_limits::max()`. Commit `b852953` uses the
  standard parenthesized call form; a new cross-platform run is required.
- Command: `make verify`
- Result: Passed all repository gates, including governance, ABI, native,
  Flutter analysis, and 28 Flutter tests.
- Skipped gate and reason: Windows and Linux TLS execution is unavailable
  locally; CI must provide final cross-platform evidence for corrected commit
  `b852953`.

## Residual risk

- Known limitation: The accepted protocol still does not register production
  ALPN bytes or a concrete certificate-size cap. The provider therefore
  enforces an exact caller-supplied ALPN token, and tests use an explicitly
  non-production token. Session code must not select production bytes until
  protocol ownership reviews them.
- Known limitation: Socket deadlines, cancellation, unauthenticated handshake
  admission limits, and teardown are session/runtime responsibilities and are
  not claimed by this provider.
- Follow-up task: XT-024 for runtime security review and protocol gaps; XT-025
  for authenticated session integration.

## Review focus

- Files or invariants requiring close review:
  `native/src/security/tls/ed25519_public_key.cpp` for canonical decode and
  prime-subgroup arithmetic; `native/src/security/tls/tls_provider.cpp` for
  certificate-callback ordering, exact pinning, disabled resumption/early data,
  negotiated-profile checks, and exporter gating; cross-platform OpenSSL 3.5.7
  CI results.

## Acceptance

- Accepted by: Pending integration-owner review
- Accepted at: Pending
- Follow-up runtime state: `review`
- Integration strategy: Pending
- Integrated SHA: Pending
- Verification reference: Local commands above; CI pending
